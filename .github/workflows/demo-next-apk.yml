name: Build TuTak Demo Next APK

on:
  push:
    branches: [demo/android-new-ui-20260817]
    paths:
      - 'demo-next/**'
      - '.github/workflows/demo-next-apk.yml'
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 45
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - name: Install dependencies
        working-directory: demo-next
        run: npm install --no-audit --no-fund
      - name: Generate Android project
        working-directory: demo-next
        run: npx expo prebuild --platform android --non-interactive
      - name: Build installable debug APK
        working-directory: demo-next/android
        run: ./gradlew assembleDebug --no-daemon
      - name: Copy APK
        run: cp demo-next/android/app/build/outputs/apk/debug/app-debug.apk tutak-demo-next.apk
      - name: Verify APK
        run: |
          test -s tutak-demo-next.apk
          head -c 2 tutak-demo-next.apk | grep -q 'PK'
          ls -lh tutak-demo-next.apk
      - uses: actions/upload-artifact@v4
        with:
          name: tutak-demo-next-apk
          path: tutak-demo-next.apk
          retention-days: 30
