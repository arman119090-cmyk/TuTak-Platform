# APK №44 собран — 10.09.2026

## Готовый файл

| | |
|---|---|
| Страница для установки | https://github.com/arman119090-cmyk/TuTak-Platform/releases/tag/apk-staging-44 |
| Прямая ссылка | `releases/download/apk-staging-44/tutak.apk` |
| Размер | 126 МБ |
| SHA-256 | `43a4ee0e1fd42597699dcdd0e89bd740372ad333d681eb6a9cd6d54b0a7e89ef` |

Открыть страницу на Android-телефоне и нажать **tutak.apk**. Android
попросит разрешение на установку из этого источника — это нормально для
приложения не из Play.

## Что в этой сборке

| | |
|---|---|
| Ветка | `fix/mobile-api-base-url-railway` |
| Коммит | `363e5925cf65e1952c68c65b64ca1f5fb293c376` |
| Профиль | `staging` |
| API | `https://tutak-api-production.up.railway.app/v1` |
| Диагностический оверлей | **отсутствует** (`diagnostics: false`) |
| Правка клавиатуры | на месте (`collapsable={false}`, из коммита `826211e`) |

Ключ подписи тот же, что у сборки №43 — локальный билд всё равно забирает
keystore из EAS по `EXPO_TOKEN`, поэтому **№44 встаёт обновлением поверх
установленного приложения**, удалять старое не нужно.

## Как собрано

Прогон **№44**: https://github.com/arman119090-cmyk/TuTak-Platform/actions/runs/34481314291

Workflow `.github/workflows/android-apk.yml`, входы:
`api_base_url = https://tutak-api-production.up.railway.app/v1`,
`profile = staging`, `builder = local`.

`builder: local` означает, что Android-часть компилировалась на раннере
GitHub, а не на ферме EAS — кредит EAS не потрачен.

Время: 13:13:37 → 13:29:39 UTC, **16 минут**. Все шаги успешно, включая
проверки самого workflow: наличие токена, пригодность адреса, чтение конфига
ровно так, как его прочитает EAS.

## Что это меняет

Три вещи по сравнению с тем, что стоит у людей сейчас:

1. **Пропадает чёрный диагностический блок** внизу экрана — он был в сборке
   профилем `diagnostic`, оставшейся с разбора проблемы с клавиатурой.
2. **Приложение обращается к Railway**, где СМС реально доставляется, а не к
   Render, где доставки не было никогда.
3. Правка клавиатуры сохранена — поле ввода не теряет фокус.

## Что проверить после установки

1. Внизу экрана нет чёрного блока с зелёным текстом; видны пять вкладок.
2. Регистрация по номеру — код приходит.
3. В «Профиле» внизу строка вида
   `TuTak · v0.1.0 · staging · tutak-api-production.up.railway.app · 363e592`
   — по ней всегда видно, какая это сборка и куда она смотрит.

Скажите, когда установите — проверю по логам Railway, что запросы идут
именно с новой сборки.
