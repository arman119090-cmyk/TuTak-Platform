# Разбор EXPORT-лога со сборки 30

**Лог:** run `QEBKD8`, 2026-09-08T13:36:50Z, commit `a1ffea4`, 381 событие,
`window 375x834 screen 375x834`.

**Статус: инициатор доказан. Конкретный native-механизм — нет.**

---

## 1. Что доказано этим логом

### 1.1. JavaScript фокус не двигает — доказано отсутствием `REQ`

Каждый `REQ focus …` в логе стоит **после** `touch` того же поля, с задержкой
120–170 мс — это штатная задержка `Pressability` в React Native:

```
 8283ms  touch phone
 8404ms  REQ focus phone      ← +121 мс, обычный тап
10173ms  touch password
10318ms  REQ focus password   ← +145 мс, обычный тап
```

Все семь `REQ` в логе — такие. **Ни одного запроса фокуса без касания.**

А вот сами аномальные переходы `REQ`-строк не имеют вообще:

```
10319ms  blur  phone t=14914
10319ms  focus password kbd=default t=14924   ← запрошен (REQ выше)
10343ms  blur  password t=14924
10343ms  focus phone kbd=number-pad t=14914   ← НИКТО не запрашивал
10365ms  blur  phone t=14914
10365ms  focus password kbd=default t=14924   ← НИКТО не запрашивал
10365ms  blur  password t=14924
10365ms  focus phone kbd=number-pad t=14914   ← НИКТО не запрашивал
```

Это **вариант B** из постановки: фокус переводит native-сторона, а React
Native только сообщает о результате.

Инструмент устроен так, что это утверждение имеет вес: перехвачены **только
команды** (`focusTextInput`/`blurTextInput`, через которые проходит любой
`ref.focus()`), а не учёт (`focusInput`, вызываемый из `onFocus`). Молчание
`REQ` — это молчание команд, а не пробел в записи.

### 1.2. Ничего не пересоздаётся — доказано постоянством native-тегов

Через весь лог: `phone t=14914`, `password t=14924`. **Оба тега неизменны.**
Значит native-view не заменялись. Плюс за всё время сбоя нет ни одной строки
`mount`, `unmount`, `win`, `scr`, `appState`.

Отпадает: remount React-компонента, пересоздание Activity, перезапуск RN
surface, замена native-view, изменение размеров окна.

### 1.3. Рисунок сбоя строго детерминирован

Каждый эпизод — **ровно четыре чередующихся перехода, всегда заканчивающихся
на `phone`**, и только потом открывается клавиатура:

```
focus password → focus phone → focus password → focus phone → kbShow on=phone
```

И это не зависит от того, куда нажали. Тапнули по `phone` — тот же рисунок.
Тапнули по `password` — тот же рисунок, и он всё равно завершается на `phone`.

Регулярность важна: случайная гонка или петля layout так себя не ведут. Это
похоже на детерминированный обход фокусируемых элементов.

### 1.4. Настоящий симптом хуже, чем «клавиатура закрывается»

**В поле «Пароль» невозможно попасть.** Нажатие по нему заканчивается фокусом
на «Номер телефона» и цифровой клавиатурой. Форма входа неработоспособна.

Двойной `kbShow` с разной высотой подтверждает, что IME успевает
переключиться:

```
10377ms  kbShow h=332 on=phone   ← клавиатура одной раскладки
10388ms  kbShow h=303 on=phone   ← и сразу другой
```

## 2. Что проверено в коде и исключено

| Проверено | Результат |
|---|---|
| `.focus()`, `.blur()`, `autoFocus`, `Keyboard.dismiss` в `apps/mobile/src` | **ноль совпадений** |
| `.focus()` в пакетах бандла: `@react-navigation/*`, `react-native-screens`, `react-native-gesture-handler`, `react-native-safe-area-context` | **ноль**; единственное совпадение во всём дереве — `HeaderSearchBar`, который здесь не рендерится |
| Откуда вообще берётся `REQ focus phone` | `TextInput` React Native сам вызывает `inputRef.current.focus()` из `usePressability` (`TextInput.js:603`, config `:629`). **Тап штатно проходит через JS-фокус** — эта строка не аномалия |
| Автозаполнение Android | `autoComplete="off"` в `TextField` компилируется в `IMPORTANT_FOR_AUTOFILL_NO` (`ReactTextInputManager.kt:652`), и то же ставит `importantForAutofill="no"`. **Оба поля уже скрыты от штатного Autofill Framework** — это довод против гипотезы автозаполнения, но не против Samsung Pass, который может работать через accessibility |
| `removeClippedSubviews` у ScrollView | не включён (проп не передаётся, native-умолчание `false`) — отсоединение поддеревьев не при чём |
| `ScrollView` снимает фокус при касании мимо поля | путь существует и у нас активен (`keyboardShouldPersistTaps="handled"`), но он вызывает `blurTextInput`, то есть дал бы строку `REQ blur`. Её в логе **нет** |

## 3. Что НЕ доказано

Причиной **не объявляется** ничего из списка: Autofill, Samsung Pass, Samsung
Keyboard, режим совместимости, складной экран, `secureTextEntry`, перерисовка
по фокусу, `elevation`/тень фокусной рамки, `KeyboardAvoidingView`,
`useEffect`. Это гипотезы, и данных, чтобы выбрать между ними, пока нет.

JS-инструментом дальше не продвинуться: он уже показал, что со стороны JS
команд нет. Следующий шаг — эксперимент на самом устройстве.

## 4. Минимальный эксперимент — без новой сборки

Две проверки в настройках телефона, каждая занимает минуту, и обе бьют точно
по оставшимся кандидатам. Сборка нужна та же (30), которая уже стоит.

**Эксперимент 1 — автозаполнение.**
Настройки → Общие настройки → Автозаполнение (Autofill service) → **Нет /
None**. Затем повторить сбой и снять EXPORT.
Если рисунок исчез — виноват сервис автозаполнения (Samsung Pass или другой),
и лечится это не выключением автозаполнения у пользователя, а свойствами
полей на нашей стороне.

**Эксперимент 2 — клавиатура.**
Переключить клавиатуру на другую (стоит Samsung Keyboard — поставить Gboard,
или наоборот). Повторить сбой, снять EXPORT.
Если рисунок исчез — дело в конкретной IME.

**Эксперимент 3 (если первые два ничего не изменили) — сложить телефон.**
Повторить на внешнем экране. Разный результат укажет на режим совместимости
раскрытого экрана.

В каждом случае нужен EXPORT и одна строка: что именно меняли.

## 5. Что нужно сообщить о телефоне

До сих пор не подтверждено и влияет на чтение лога:

* модель (Galaxy Z Fold какого поколения);
* версия Android и One UI;
* какая клавиатура стоит;
* сложен или раскрыт был телефон;
* включён ли режим совместимости для приложения.

`window 375x834` при `screen 375x834` — это окно приложения в **логических**
единицах, и оно не менялось за весь лог. На прошлом снимке главного экрана
было `411x882`. Разные состояния экрана дают разные числа, поэтому состояние
телефона в момент съёмки — не деталь.

## 6. Проверки

| Проверка | Результат |
|---|---|
| `apps/mobile` tests | **361 / 361**, 43 сюиты |
| `apps/mobile` typecheck | pass |
| `apps/mobile` lint | pass |
| `scripts/build-demo-app.sh` | pass, `demo/` без дрейфа |
| Commit | `08def52c6dbc9fb3bd94e2b11342301a10978962` |

Правок поведения не вносилось. Merge и deploy не выполнялись. Viva и PR №30
не затронуты.

## 7. Чего ещё нет

* Проверки на физическом устройстве после исправления — **исправления нет**,
  потому что причина не доказана.
* Regression-теста — писать его не под что: тест под недоказанную причину
  закрепил бы догадку.
* iOS — **NOT VERIFIED**.
