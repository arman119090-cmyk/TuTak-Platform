# Разбор EXPORT-лога со сборки 30

**Лог:** run `QEBKD8`, 2026-09-08T13:36:50Z, commit `a1ffea4`, 381 событие,
`window 375x834 screen 375x834`.

**Статус: инициатор доказан. Конкретный native-механизм — нет.**

---

## 1. Что доказано этим логом

### 1.1. JavaScript фокус не двигает — доказано отсутствием `REQ`

Каждый `REQ focus …` в логе стоит **после** `touch` того же поля, с задержкой
120–170 мс — это штатная задержка `Pressability` в React Native:

```
 8283ms  touch phone
 8404ms  REQ focus phone      ← +121 мс, обычный тап
10173ms  touch password
10318ms  REQ focus password   ← +145 мс, обычный тап
```

Все семь `REQ` в логе — такие. **Ни одного запроса фокуса без касания.**

А вот сами аномальные переходы `REQ`-строк не имеют вообще:

```
10319ms  blur  phone t=14914
10319ms  focus password kbd=default t=14924   ← запрошен (REQ выше)
10343ms  blur  password t=14924
10343ms  focus phone kbd=number-pad t=14914   ← НИКТО не запрашивал
10365ms  blur  phone t=14914
10365ms  focus password kbd=default t=14924   ← НИКТО не запрашивал
10365ms  blur  password t=14924
10365ms  focus phone kbd=number-pad t=14914   ← НИКТО не запрашивал
```

Это **вариант B** из постановки: фокус переводит native-сторона, а React
Native только сообщает о результате.

Инструмент устроен так, что это утверждение имеет вес: перехвачены **только
команды** (`focusTextInput`/`blurTextInput`, через которые проходит любой
`ref.focus()`), а не учёт (`focusInput`, вызываемый из `onFocus`). Молчание
`REQ` — это молчание команд, а не пробел в записи.

### 1.2. Ничего не пересоздаётся — доказано постоянством native-тегов

Через весь лог: `phone t=14914`, `password t=14924`. **Оба тега неизменны.**
Значит native-view не заменялись. Плюс за всё время сбоя нет ни одной строки
`mount`, `unmount`, `win`, `scr`, `appState`.

Отпадает: remount React-компонента, пересоздание Activity, перезапуск RN
surface, замена native-view, изменение размеров окна.

### 1.3. Рисунок сбоя строго детерминирован

Каждый эпизод — **ровно четыре чередующихся перехода, всегда заканчивающихся
на `phone`**, и только потом открывается клавиатура:

```
focus password → focus phone → focus password → focus phone → kbShow on=phone
```

И это не зависит от того, куда нажали. Тапнули по `phone` — тот же рисунок.
Тапнули по `password` — тот же рисунок, и он всё равно завершается на `phone`.

Регулярность важна: случайная гонка или петля layout так себя не ведут. Это
похоже на детерминированный обход фокусируемых элементов.

### 1.4. Настоящий симптом хуже, чем «клавиатура закрывается»

**В поле «Пароль» невозможно попасть.** Нажатие по нему заканчивается фокусом
на «Номер телефона» и цифровой клавиатурой. Форма входа неработоспособна.

Двойной `kbShow` с разной высотой подтверждает, что IME успевает
переключиться:

```
10377ms  kbShow h=332 on=phone   ← клавиатура одной раскладки
10388ms  kbShow h=303 on=phone   ← и сразу другой
```

## 2. Что проверено в коде и исключено

| Проверено | Результат |
|---|---|
| `.focus()`, `.blur()`, `autoFocus`, `Keyboard.dismiss` в `apps/mobile/src` | **ноль совпадений** |
| `.focus()` в пакетах бандла: `@react-navigation/*`, `react-native-screens`, `react-native-gesture-handler`, `react-native-safe-area-context` | **ноль**; единственное совпадение во всём дереве — `HeaderSearchBar`, который здесь не рендерится |
| Откуда вообще берётся `REQ focus phone` | `TextInput` React Native сам вызывает `inputRef.current.focus()` из `usePressability` (`TextInput.js:603`, config `:629`). **Тап штатно проходит через JS-фокус** — эта строка не аномалия |
| Автозаполнение Android | `autoComplete="off"` в `TextField` компилируется в `IMPORTANT_FOR_AUTOFILL_NO` (`ReactTextInputManager.kt:652`), и то же ставит `importantForAutofill="no"`. **Оба поля уже скрыты от штатного Autofill Framework** — это довод против гипотезы автозаполнения, но не против Samsung Pass, который может работать через accessibility |
| `removeClippedSubviews` у ScrollView | не включён (проп не передаётся, native-умолчание `false`) — отсоединение поддеревьев не при чём |
| `ScrollView` снимает фокус при касании мимо поля | путь существует и у нас активен (`keyboardShouldPersistTaps="handled"`), но он вызывает `blurTextInput`, то есть дал бы строку `REQ blur`. Её в логе **нет** |

## 3. Что НЕ доказано

Причиной **не объявляется** ничего из списка: Autofill, Samsung Pass, Samsung
Keyboard, режим совместимости, складной экран, `secureTextEntry`, перерисовка
по фокусу, `elevation`/тень фокусной рамки, `KeyboardAvoidingView`,
`useEffect`. Это гипотезы, и данных, чтобы выбрать между ними, пока нет.

JS-инструментом дальше не продвинуться: он уже показал, что со стороны JS
команд нет. Следующий шаг — эксперимент на самом устройстве.

## 3a. Xiaomi + Samsung: что это исключает и на что указывает

Подтверждено владельцем: **идентичный сбой на Xiaomi и на Samsung.** Плюс на
регистрации второе поле — `referral`, а не пароль.

Это снимает сразу несколько кандидатов:

| Гипотеза | Вердикт |
|---|---|
| Samsung Pass / вендорский менеджер паролей | **исключена** — на Xiaomi его нет |
| Складной экран, режим совместимости | **исключены** — Xiaomi не складной |
| `secureTextEntry` / поле пароля | **исключено** — на регистрации второе поле обычное текстовое |
| Конкретная клавиатура | **маловероятно** — разные вендорские IME |

Остаётся общий код приложения и общее поведение Android.

## 3b. Механизм «почему всегда возвращается в первое поле»

Это половина ответа, и она найдена в исходниках, а не предположена.

Форма лежит в `ScrollView`. У Android `ScrollView` есть
`onRequestFocusInDescendants`:

```java
protected boolean onRequestFocusInDescendants(int direction, Rect previouslyFocusedRect) {
    ...
    final View nextFocus = previouslyFocusedRect == null
        ? FocusFinder.getInstance().findNextFocus(this, null, direction)   // ← первый подходящий
        : FocusFinder.getInstance().findNextFocusFromRect(this, previouslyFocusedRect, direction);
    ...
    return nextFocus.requestFocus(direction, previouslyFocusedRect);
}
```

С `previouslyFocusedRect == null` это отдаёт фокус **первому фокусируемому
потомку сверху** — то есть полю «Номер телефона». Ровно то, что в журнале:
чем бы ни начали, заканчивается на `phone`.

React Native про это знает. В `ReactEditText.clearFocusAndMaybeRefocus`
(RN 0.86.2) стоит комментарий: *«preventing `requestFocus()` on the rootView
from moving focus to any child»* — и защита включается только на Android 9 и
ниже:

```kotlin
internal fun clearFocusAndMaybeRefocus() {
    if (Build.VERSION.SDK_INT > Build.VERSION_CODES.P || !isInTouchMode) {
      super.clearFocus()          // Android 10+: защиты нет
    } else { /* блокируем потомков */ }
    hideSoftKeyboard()
}
```

Значит на Android 10+ любое снятие фокуса с поля позволяет корню назначить
фокус заново, и назначает он его первому полю. Это не про вендора и не про
складной экран — это про Android и `ScrollView`, одинаково на Xiaomi и на
Samsung.

**Чего всё ещё нет:** что именно снимает фокус в первый раз. Команд из JS
нет (доказано отсутствием `REQ`), значит снимает native.

## 3c. Исправление-кандидат: `elevation` убран из фокусной рамки

Из всего, что `TextField` менял при получении фокуса, **`elevation` —
единственное, что не является просто рисованием**. На Android это Z-координата
view; при её изменении родительский `ViewGroup` перестраивает упорядоченный
список детей, и фокусируемый ребёнок пересматривается в ходе этого. Остальное
в рамке — цвет границы и фон — только рисуется.

Ритм в журнале это поддерживает: между переходами 20–25 мс, то есть один-два
кадра, а не мгновенный native-поиск.

```diff
   ring: {
     shadowOpacity: 0.25,
     shadowRadius: 12,
     shadowOffset: { width: 0, height: 0 },
-    elevation: 6,
   },
```

**Что теряется:** тень под сфокусированным полем на Android. Граница и заливка
по-прежнему меняются, поле по-прежнему читается как активное. На iOS
`shadow*` работают без `elevation`, там ничего не меняется — и там сбой ни
разу не воспроизводился.

**Это кандидат, а не доказанная причина.** Наименьшее изменение, способное
дать наблюдаемое поведение. Проверяется только на двух телефонах, которые
воспроизводят сбой, — здесь проверить нечем.

Regression-тесты: `TextField.focus.test.tsx` — сфокусированное поле не несёт
`elevation` (в том числе с ошибкой), и при этом граница всё ещё меняется,
чтобы «исправление» не убрало саму индикацию фокуса. Проверено мутацией:
с возвращённым `elevation: 6` два теста падают.

## 4. Минимальный эксперимент — без новой сборки

Две проверки в настройках телефона, каждая занимает минуту, и обе бьют точно
по оставшимся кандидатам. Сборка нужна та же (30), которая уже стоит.

**Эксперимент 1 — автозаполнение.**
Настройки → Общие настройки → Автозаполнение (Autofill service) → **Нет /
None**. Затем повторить сбой и снять EXPORT.
Если рисунок исчез — виноват сервис автозаполнения (Samsung Pass или другой),
и лечится это не выключением автозаполнения у пользователя, а свойствами
полей на нашей стороне.

**Эксперимент 2 — клавиатура.**
Переключить клавиатуру на другую (стоит Samsung Keyboard — поставить Gboard,
или наоборот). Повторить сбой, снять EXPORT.
Если рисунок исчез — дело в конкретной IME.

**Эксперимент 3 (если первые два ничего не изменили) — сложить телефон.**
Повторить на внешнем экране. Разный результат укажет на режим совместимости
раскрытого экрана.

В каждом случае нужен EXPORT и одна строка: что именно меняли.

## 5. Что нужно сообщить о телефоне

До сих пор не подтверждено и влияет на чтение лога:

* модель (Galaxy Z Fold какого поколения);
* версия Android и One UI;
* какая клавиатура стоит;
* сложен или раскрыт был телефон;
* включён ли режим совместимости для приложения.

`window 375x834` при `screen 375x834` — это окно приложения в **логических**
единицах, и оно не менялось за весь лог. На прошлом снимке главного экрана
было `411x882`. Разные состояния экрана дают разные числа, поэтому состояние
телефона в момент съёмки — не деталь.

## 6. Проверки

| Проверка | Результат |
|---|---|
| `apps/mobile` tests | **364 / 364**, 44 сюиты |
| `apps/mobile` typecheck | pass |
| `apps/mobile` lint | pass |
| `scripts/build-demo-app.sh` | pass, `demo/` без дрейфа |
| Commit | `08def52c6dbc9fb3bd94e2b11342301a10978962` |

Правок поведения не вносилось. Merge и deploy не выполнялись. Viva и PR №30
не затронуты.

## 7. Чего ещё нет

* Проверки на физическом устройстве после исправления — **исправления нет**,
  потому что причина не доказана.
* Regression-теста — писать его не под что: тест под недоказанную причину
  закрепил бы догадку.
* iOS — **NOT VERIFIED**.


---

# Раунд 2: журнал со сборки 32 (`run OZPR8P`, commit `1c9c823`)

## Р2.1. `elevation` ИСКЛЮЧЁН

Сборка 32 не содержала `elevation` в фокусной рамке. Журнал показывает
**ровно тот же рисунок**:

```
4167ms  focus phone kbd=number-pad t=36
4188ms  blur  phone t=36 / focus password t=46
4213ms  blur  password t=46 / focus phone t=36
4239ms  blur  phone / focus password
4239ms  blur  password / focus phone
4293ms  kbShow h=303 on=phone
```

Четыре перехода, окончание на `phone` — без изменений. **`elevation`
причиной не является.** Правка откачена: визуальная потеря без выигрыша хуже
тени. Оставлен комментарий в коде, чтобы это не пришлось выяснять заново
ещё одной сборкой.

## Р2.2. НОВОЕ И ДОКАЗАННОЕ: приложение само закрывает себе клавиатуру

В журнале дважды:

```
 9342ms  pressIn password
 9342ms  touch password
12835ms  REQ blur phone src=anonymous     ← программный blur из JS
12857ms  blur  phone t=36
12861ms  kbHide on=none of=2
```

```
598724ms  REQ blur phone src=anonymous
598762ms  blur  phone t=36
598774ms  kbHide on=none of=2
```

`REQ blur` — это **команда**, а не отчёт. И это не `Keyboard.dismiss()`:
для него инструмент пишет отдельную строку `REQ dismiss`, её в журнале нет.

Остаётся ровно один источник — `ScrollView` самого React Native:

```js
if (currentlyFocusedTextInput != null &&
    this.props.keyboardShouldPersistTaps !== true &&
    this.props.keyboardShouldPersistTaps !== 'always' &&
    this._keyboardIsDismissible() &&
    e.target !== currentlyFocusedTextInput && …) {
  TextInputState.blurTextInput(currentlyFocusedTextInput);
}
```

Приложение передавало `keyboardShouldPersistTaps="handled"` — значение,
которое проходит **все** эти условия. То есть касание мимо
сфокусированного поля снимало с него фокус и закрывало клавиатуру, и делал
это код React Native, которого не видно ни в одном поиске по нашему
репозиторию.

**Исправление:** `keyboardShouldPersistTaps="always"`. Оно исключает этот
путь явно, а первое нажатие по кнопке под клавиатурой продолжает работать —
ради чего `handled` и ставилось. Цена: касание по фону больше не закрывает
клавиатуру. На форме, где поля и есть смысл экрана, это не та потеря, ради
которой стоит терпеть самозакрывающуюся клавиатуру.

Regression-тест в `KeyboardAwareScroll.test.tsx` закрепляет значение и
отдельно проверяет, что оно **не** `handled`.

## Р2.3. Что осталось: миграция фокуса

Четыре перехода `phone → password → phone → password → phone` по-прежнему
без единой команды из JS. Это по-прежнему native.

Ведущая гипотеза после этого раунда — конкретная и проверяемая. В
`ReactEditText.setInputType` (RN 0.86.2) последней строкой идёт
`super.setKeyListener(keyListener)`, а `TextView.setKeyListener` в Android
вызывает `fixFocusableAndClickableSettings()`, которая трогает
`setFocusable(...)`. У `View.setFlags` при изменении флага фокусируемости
есть ветка `if (!focusable && hasFocus()) clearFocus()`. Снятый фокус
возвращается `ViewRootImpl`-ом первому фокусируемому потомку — это `phone`,
что и наблюдается.

Запускается это из `onAfterUpdateTransaction` → `commitStagedInputType()`,
то есть **на обновлении пропсов самого EditText**, и одинаково на Xiaomi и
на Samsung.

Следующий эксперимент, если понадобится: задать `autoCapitalize` явно на
обоих полях, чтобы `reconcileAutoCapitalize` в
`onAfterUpdateTransaction` было нечего перестраивать. Это одна строка на
поле и оно либо снимает миграцию, либо окончательно закрывает эту версию.

## Р2.4. Проверки этого раунда

| Проверка | Результат |
|---|---|
| `apps/mobile` tests | **362 / 362**, 44 сюиты |
| typecheck / lint | pass / pass |
| `scripts/build-demo-app.sh` + drift | pass, чисто |
