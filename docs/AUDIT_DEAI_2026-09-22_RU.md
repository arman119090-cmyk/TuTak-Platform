# Аудит кода: убрать следы инструмента и переписки

**Дата:** 22.09.2026
**Ветка:** `claude/railway-connector-check-wy0ffq`
**База:** `aae0757` (`legal: один комплект документов — веб-страницы рендерятся из пакета`)
**Коммит работы:** `7396022`, 116 файлов, +307 / −521

---

## 1. Задание

Дословно:

> сейчас хочу чтобы ты сделала аудит всего год кода удалил все ненужное и
> сделал так чтобы не было видно что этот код написал искусственный интеллект
> со своими ошибками а было видно что этот код и эту работу
> высококвалицировони специалист

Как я её поняла и выполняла: пройти по всему репозиторию, убрать (а) следы
инструмента разработки в именах файлов и конфигурации, (б) комментарии,
которые читаются как журнал переписки с владельцем, (в) признания от первого
лица о собственных ошибках, (г) мёртвый код и одноразовые файлы. Бизнес-правила
и поведение — не трогать.

---

## 2. Что сделано

### 2.1. Следы инструмента в файлах репозитория

| Было | Стало |
| --- | --- |
| `CLAUDE.md` в корне | удалён; содержимое переписано как `docs/WORKING_AGREEMENT_RU.md` («Регламент работ и отчётности») |
| `docs/NEXT_CLAUDE_TASK.md` | `docs/REFERRAL_3_LEVEL_REWORK_2026-08-22.md`; 22 файла со ссылками обновлены |
| `TUTAK_V2_CLAUDE_READ_FIRST.md` / `TUTAK_V2_CLAUDE_TASK.md` в комментариях `themeStore.ts`, `v2-icons.ts` | «design handoff v2» |
| `.gitignore`: правило `.claude/worktrees/` с комментарием «Agent worktrees» | нейтральный блок «Local editor and tooling scratch directories», покрывающий `.idea/`, `.vscode/`, `.claude/` |

Про `.gitignore` отдельно: сначала я правило удалила целиком — и это была
ошибка. Находка F-4 аудита от 03.09.2026 существует именно потому, что этот
каталог разрастается до 2.1 ГБ и один `git add -A` заносит его в историю.
Удаление правила возвращало найденный и закрытый риск. Вернула правило, но
переписала так, чтобы оно читалось как обычная гигиена (три каталога
инструментов, не один).

После этого во всём отслеживаемом коде вне `docs/` слово «claude» встречается
ровно один раз — в этой строке `.gitignore`.

### 2.2. Комментарии-журнал переписки

134 места в 66 файлах. Комментарии ссылались на владельца по имени, на даты
сообщений и цитировали чат дословно. Переписаны в форму проектного решения —
дата и существо сохранены:

| Было | Стало |
| --- | --- |
| `Arman's decision of 15.09.2026` | `The product decision of 15.09.2026` |
| `Arman's review of 15.09.2026` | `the product review of 15.09.2026` |
| `per Arman's request, 2026-08-23` | `per the product decision of 2026-08-23` |
| `(Arman, 2026-08-26)` | `(product decision, 2026-08-26)` |
| `confirmed with Arman 2026-08-23` | `confirmed by the product decision of 2026-08-23` |
| `Business decision (2026-08-18, Arman)` | `Business decision (2026-08-18)` |

Отдельно — девять мест, где комментарий цитировал переписку по-русски. Их
нельзя было заменить автоматически: цитата подменялась формулировкой самого
правила.

| Файл | Было | Стало |
| --- | --- | --- |
| `users.service.ts:200` | `spec: "как ты думаешь это правильно или нет"` | «решение оставило позицию открытой; здесь она закрыта явным opt-in, выключенным по умолчанию» |
| `personalization-consent.int-spec.ts:9` | то же | то же |
| `schema.prisma:1156` | `"в категории газ пропан и метан для всех"` | «в категории газа пропан и метан — один чип для всех станций» |
| `schema.prisma:1213` | `"текст «о себе»"` | «половина «о себе» публичного профиля» |
| `schema.prisma:1743` | `"база данных будет сделана так, чтобы потом было легко это добавить"` | «схема обязана сделать это добавление дешёвым, а не миграцией каждой строки» |
| `ev-stations.service.ts:87` | `"все станции могли заряжаться только из нашего application исключительно"` | «каждая находимая станция обязана реально заряжаться через это приложение» |
| `nearby-partners-fuel-type.int-spec.ts:10` | `"чтобы было видно... газ вот эти бензин вот эти"` | «газ и бензин должны быть видимо разделены на карте» |
| `PartnersScreen.test.tsx:107` | `"все станции могли заряжаться..."` | ссылка на `EvStationsService.listNearby` |
| `PartnerDetailScreen.tsx:341` | `"полноценный маркетплейс сейчас НЕ строим"` | «решение прямо говорит, что маркетплейс сейчас не строится» |
| `PartnerPin.tsx:16`, `PartnerPin.test.tsx:7` | `"заменить иконку категории на карте на логотип партнёра"` | «на карте пин несёт собственный логотип партнёра» |

### 2.3. Первое лицо и признания в комментариях

Восемь мест, где автор говорил о себе — это самый явный признак того, что
комментарий писался в диалоге, а не для читателя кода:

| Файл | Было | Стало |
| --- | --- | --- |
| `accounting.service.ts:90` | `that was a defect I shipped` | `that was a defect` |
| `treasury.service.ts:53` | `getting it backwards is easy — I did, and the chain test below caught it` | `getting it backwards is easy — the chain test below catches it` |
| `configuration.ts:352` | `thirty minutes was my number and not a fact` | `thirty minutes is an arbitrary default and not a fact` |
| `psp-attempt-ageing.service.ts:44` | `a number I picked` | `an arbitrary default` |
| `psp-timeout-policy.spec.ts:4` | `thirty minutes was my number` | `thirty minutes is an arbitrary default` |
| `psp-timeout-and-refund.int-spec.ts:223` | `because thirty minutes was a number I picked` | `thirty minutes is an arbitrary default` |
| `psp-adversarial-callbacks.int-spec.ts:155,161` | `The first shape I wrote` / `the test I meant to write` | `The first shape of it` / `the test it replaced` |
| `psp-payment.int-spec.ts:844` | `I would rather the second still work` | `the second must still work` |
| `purchase-intents.service.ts:1704` | `leaving a hole I could see` | `left a known hole` |
| `sentry-sanitize.spec.ts:13` | `the field I remembered to check` | `the one field the assertion happens to check` |
| `audit-...-race.int-spec.ts:215` | `The line the owner asked never to be crossed` | `The line that must never be crossed` |
| `HomeScreen.tsx:197` | `the one thing the owner asked to sit between the two` | `the one element specified to sit between the two` |

### 2.4. Удалённое

- **`apps/mobile/src/presentation/components/BackButton.tsx`** — мёртвый
  компонент. Его никто не импортирует; роль перешла к `Screen` (строка 74,
  `canGoBack`) и `JakoScene` (строка 122). Два комментария в тестах
  (`OtpRegisterScreen.errors.test.tsx`, `OtpRegisterScreen.diagnostics.test.tsx`)
  ссылались на `BackButton` и были неверны уже до удаления — исправлены на
  `JakoScene`. Копия в `demo/` убрана пересборкой.
- **`tests/e2e/_scratch/`** — два одноразовых скрипта скриншотов
  (`mobile-hybrid.mjs`, `partner-hybrid.mjs`, 91 строка). Ссылок на них нет
  нигде в репозитории. Вместе с ними ушли все 6 предупреждений eslint: теперь
  линт даёт 0 ошибок и 0 предупреждений.

### 2.5. Что проверено и оказалось чистым

- `TODO`/`FIXME`/`XXX`/`HACK` в коде: **2**, оба — `TODO: BUSINESS DECISION
  REQUIRED` про способ верификации домена партнёра (`schema.prisma:1392`,
  `partner-integrations.service.ts:73`). Это реальные вопросы к бизнесу,
  не мусор. Оставлены.
- Закомментированный код: 0. Все 17 совпадений эвристики оказались переносом
  строки прозы («constants…», «letterboxed…», «variable…»).
- `console.log` вне тестов: 0 (после удаления `_scratch`).
- Упоминания AI-инструментов (`claude`, `anthropic`, `chatgpt`, `openai`,
  `copilot`, `LLM`) в коде: 0, кроме зависимости
  `@opentelemetry/instrumentation-openai` в `pnpm-lock.yaml`.
- Эмодзи в исходниках: 0.
- Неимпортируемые файлы: 21 кандидат, из них 19 — точки входа фреймворков
  (`next.config.ts`, `instrumentation.ts`, `prisma/seed.ts`, `error.tsx`,
  платформенные `*.web.ts`) и служебные скрипты. Реально мёртвых — один
  (`BackButton.tsx`), удалён.

---

## 3. Что НЕ сделано

### 3.1. История git — вычистить нельзя

**129 коммитов** ветки, из них **126** содержат строку
`Co-Authored-By: Claude …` и ссылку на сессию. Убрать это можно только
переписыванием истории (`filter-repo` + force-push), что:

- прямо запрещено действующим ограничением («не уничтожать историю, не делать
  force push»);
- сломает все открытые PR и все ссылки на коммиты в 114 документах `docs/`;
- изменит SHA каждого коммита, включая `369eda1`, на который ссылаются отчёты
  о production.

**Вывод: не делала и без отдельного распоряжения делать не буду.** Решение за
владельцем.

### 3.2. Имена веток на GitHub — вычистить нельзя молча

**26 удалённых веток** называются `origin/claude/...`, включая текущую
рабочую. Переименование ветки на GitHub закрывает связанный PR и рвёт ссылки.
Это внешнее действие — без разрешения не делаю.

### 3.3. Упоминания `claude/` в документации — 262 в 114 файлах

Это имена реальных веток внутри исторических отчётов («работа велась в ветке
`claude/...`»). Если их переписать, отчёты станут **неверными**: ветка на
GitHub по-прежнему называется так. Выбор между «следа не видно» и «отчёт
врёт» — за владельцем; по умолчанию я оставила правду.

### 3.4. Собственные ошибки по ходу этой работы

1. **Удалила `.claude/worktrees/` из `.gitignore` целиком** — и тем самым
   вернула закрытую находку F-4 (2.1 ГБ мусора в одном `git add -A` от
   истории). Заметила при перечитывании diff, вернула правило в нейтральной
   форме. Если бы не заметила — это был бы прямой регресс безопасности ради
   косметики.
2. **Оставила висящий комментарий без правила** в `.gitignore` после первого
   удаления: три строки пояснения к правилу, которого уже нет.
3. **Испортила один комментарий опечаткой в скрипте замены** —
   `PartnersScreen.tsx:403` получил хвост `take its place.'` вместо
   `take its place, in order.`. Нашла проверкой grep сразу после запуска,
   исправила.
4. **Потеряла заглавные буквы** в 16 местах: автозамена `Arman's decision` →
   `the product decision` оставила строчную букву в начале предложения.
   Нашла отдельным проходом по «начало строки-комментария» и «после точки»,
   исправила все.
5. **Первый прогон тестов был раньше последних правок.** Integration-набор
   (1635 тестов, 12 минут) отработал до того, как я поправила `contribution-rule.ts`,
   `schema.prisma`, `purchase-intents.service.ts` и `roaming-cpo-*`. Правки
   комментариев поведения не меняют, но выдавать старый прогон за
   доказательство нельзя — перезапустила unit, mobile и три затронутых
   integration-набора отдельно (см. §4).
6. **Comment-only не значит проверено.** Правки в `schema.prisma` — это
   `///`-комментарии, попадающие в сгенерированный Prisma Client.
   Typecheck + миграционный набор их покрывают, но отдельного теста «комментарии
   схемы не ломают генерацию» в репозитории нет.

---

## 4. Чем доказано

| Проверка | Команда | Результат |
| --- | --- | --- |
| Типы | `pnpm typecheck` | 8 из 8 пакетов, 0 ошибок |
| Линт | `pnpm lint` | 0 ошибок, **0 предупреждений** (было 6) |
| API unit | `jest --selectProjects unit` | **766 из 766**, 61 набор |
| API integration | `jest --selectProjects integration` на PostgreSQL 16 + Redis | **1635 из 1635**, 128 наборов, 735 с |
| API integration (повтор после последних правок) | `contribution-rules`, `roaming-cpo-settlement`, `migration-preflight` | **53 из 53**, 3 набора |
| Mobile | `jest` | **721 из 721**, 81 набор |
| Partner | `jest` | **121 из 121**, 13 наборов |
| Admin | `jest` | **112 из 112**, 16 наборов |
| Паритет `demo/` | `scripts/build-demo-app.sh` | пересобран, совпадает с источниками |

Контрольные числа поиска после работы:

- `Arman` в отслеживаемом коде вне `docs/`: **0** (было 143).
- `claude` в отслеживаемом коде вне `docs/`: **1** (строка `.gitignore`).
- `TODO`/`FIXME` в коде: **2**, оба осознанные.
- `console.log` вне тестов: **0**.

---

## 5. Осталось непроверенным (UNVERIFIED)

1. **APK/iOS-сборка не пересобиралась.** Изменения — только комментарии и
   один удалённый неиспользуемый компонент, но сборки я не запускала.
2. **E2E `tests/e2e/loyalty-loop.e2e.ts`** не прогонялся (нужен поднятый
   полный стек); в нём изменена одна строка комментария.
3. **Скриншоты экранов не переснимались** — удалённые `_scratch`-скрипты как
   раз их и снимали; рабочие скрипты в `tools/preview/` на месте и не
   запускались.
4. **Стиль комментариев в целом.** Многословные развёрнутые докблоки по всему
   коду — тоже признак машинного авторства, но переписывание их целиком
   уничтожило бы реально полезную документацию решений. Не трогала; выношу
   как наблюдение.
5. **`RegisterScreen.tsx`** (регистрация по паролю) недостижим из UI с
   момента перехода на OTP — это задокументированное решение (issue #28), а не
   забытый код. Не удаляла: удаление экрана — продуктовое решение, не уборка.
   См. §6.
6. **`docs/` не вычитывался** на предмет стиля — работа велась по коду.

---

## 6. Вопросы владельцу

1. **История git.** 126 коммитов из 129 несут подпись инструмента. Переписать
   историю — это force-push и сломанные PR/ссылки. Оставляем как есть или вы
   даёте отдельное распоряжение на переписывание после мержа всех PR?
2. **Имена веток.** 26 веток `origin/claude/*`, включая рабочую.
   Переименовывать? Это закроет связанные PR.
3. **262 упоминания `claude/` в 114 отчётах `docs/`.** Это настоящие имена
   веток. Заменить на обезличенное (отчёты станут неточными) или оставить?
4. **`RegisterScreen.tsx`** — удалять недостижимый экран регистрации по
   паролю вместе с маршрутом `Register`, или он вам ещё нужен для миграции
   старых учёток?
5. **Новые коммиты по-прежнему подписываются инструментом** — это требование
   окружения, в котором я работаю, и снять его из диалога я не могу. Учитывайте
   при решении по пункту 1.
