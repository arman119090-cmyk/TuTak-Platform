# Исполнение чек-листа READY — что закрыто 21.09.2026 (вторая половина дня)

## 1. Задание

Владелец (21.09.2026): «Всё, что ты из этих пунктов можешь закрыть — закрывай, доделывай всё,
что уверен, что сможешь; но перед тем, как доделать, скажи, какие пункты ты можешь закрыть».
Список пунктов — `READINESS_CHECKLIST_2026-09-21_RU.md` (39 пунктов, READY-A / READY-B).
Объявлено до начала: полностью закрываю 2, 9, 10, 14, 16, 27, 37; продвигаю 3, 4, 5, 15, 31;
не могу 1, 6, 7, 8, 11, 12, 13, 17, 18–26, 28, 29, 32, 33, 35, 36, 38.

Ограничения прежние: production не менять, ничего не merge-ить и не деплоить без отдельного
поручения, реальные оплаты/SMS не запускать, секреты не публиковать.

## 2. База

- Production: `main` `369eda1` (без изменений).
- Исходные ветки: `claude/dark-theme` `4c4827e` (содержит #60 `2eac071`), #62 `b1a809f`,
  #61 `235d9b5`, #65 `7f93a23` (содержит #64 `aec987b`, #63 `333b9f4`).
- Новая ветка: `claude/rc-20260921` — `631b5cd` (merge #62), `f853e2e` (merge #65 с #64/#63),
  `a3f6786` (demo), `dfd5a5a` (LockScreen на Jako), `10d5d00` (docs + `backup.sh`).
- PR: #66 (`dark-theme` → база #60, пункт 2), #67 (RC → `main`, **черновик**, пункты 3–5).

## 3. Что сделано — по пунктам

| № | Итог | Доказательство |
| --- | --- | --- |
| 2 | ✅ PR #66 открыт (9 коммитов поверх #60) | PR #66 |
| 3, 4, 5 | 🟡→ готово к merge: RC-ветка со всеми PR, конфликты разрешены (`.env.example` ×2, 5 auth-экранов: import-блоки `JakoScene`/`DataSafeNote` + `localPhoneDigits`), LockScreen-ветка D07/D08 переписана на `JakoScene` (после #61 `SafeAreaView`/wing mark в этом экране нет) | CI #67 10/10; локально integration 125 suites 1594/1594, unit 750, mobile 78/709, admin 112+4, partner 121, tsc/eslint 0 везде, `migrate diff` пуст, demo parity |
| 9 | ✅ локально: backup 1.0 с → restore 1.6 с → verify PASS 3.0 с на базе с 21 569 проводками; исправлена гонка в `scripts/backup.sh` | `OPS_EVIDENCE_2026-09-21_RU.md` п. 9 |
| 10 | ✅ измерено на production edge: XFF заменяется, 2 записи, `CLIENT_IP_TRUSTED_HOPS=2` = настроенному | workflow run 35602403969 + deploy-лог tutak-api |
| 14 | ✅ 8 миграций expand-only; код `main` на схеме RC: boot 200, smoke идентичен контролю; процедура отката записана | `OPS_EVIDENCE` п. 14 |
| 15 | 🟡 нагрузка на коде RC локально: 0 ошибок, ledger 0.0000; стенд Railway не проверялся | `OPS_EVIDENCE` п. 15 |
| 16 | ✅ Railway Redis: том + `--save 60 1`, без AOF; тест: штатный рестарт сохраняет очереди, `kill -9` теряет ≤ 60 с; рекомендация `--appendonly yes` | `OPS_EVIDENCE` п. 16 |
| 27 | 🟡 инструкция `DAILY_RECONCILIATION_RU.md`; человека назначает владелец | файл |
| 31 | см. раздел 7 (APK) | run 35602999208 |
| 37 | 🟡 шаблоны `SUPPORT_TEMPLATES_RU.md`; канал назначает владелец | файл |

Файлы: `docs/OPS_EVIDENCE_2026-09-21_RU.md`, `docs/DAILY_RECONCILIATION_RU.md`,
`docs/SUPPORT_TEMPLATES_RU.md`, `docs/READINESS_CHECKLIST_2026-09-21_RU.md` (статусы),
`docs/LOAD_TEST.md` (флаг), `docs/README.md` (индекс), `scripts/backup.sh`,
`apps/mobile/src/presentation/screens/appLock/LockScreen.tsx`, `apps/api/.env.example`,
5 auth-экранов (только импорты), `demo/`.

## 4. Что НЕ сделано

### 4.1 Не мог по ограничениям
- Merge в `main` (пункты 1, 3, 4, 5 до конца) — это деплой production; только по слову владельца.
- 11 (S3-тест) — значения ключей недоступны (list-variables блокируется), а API-путь требует
  admin-логина в production, которого у меня нет и который я не должен использовать.
- 6, 7, 8, 12, 13, 17, 18–26, 28, 29, 32, 33, 35, 36, 38 — только владелец/внешние стороны.

### 4.2 Не дошёл / не получилось
- Нагрузка на стенде Railway (нет staging-окружения) — п. 15 остаётся 🟡.
- RPO/RTO на Railway с реальным объёмом — не измерялись (production не трогали).
- Денежный путь **старым** кодом на новой схеме: smoke-скрипт упёрся в 403 на
  `wallet/admin/adjust` одинаково на обеих схемах — это устаревший `scripts/smoke-test.sh`,
  не схема; сам скрипт не чинил (вне поручения, P2, см. п. 6 чек-листа).
- Alert outbox, AOF на Railway Redis — рекомендации, не изменения (production-сервис).

### 4.3 Собственные ошибки по ходу
- **Дважды убил собственный фоновый прогон** `pkill -f "<pattern>"` — шаблон совпадал с
  командной строкой самой оболочки (exit 144). Потерян один запуск integration (~10 мин).
  Дальше — `pgrep -f "[m]ain…"`.
- **Скрипт отката с `$DB` в одинарных кавычках**: первый «контрольный» прогон стартовал с
  базой `$DB` и не поднялся (P1003), а «откатный» прогон в тот момент отвечал старым процессом
  на том же порту — результат того захода был недействителен; переделал с чистыми базами и
  разными портами, `diff` двух smoke-логов пуст.
- **`grep -rn … $(grep -l …)` без файлов читал stdin** и завис на 120 с.
- **Первый запуск нагрузки без `CARD_PAYMENTS_ENABLED=true`** упал на `PaymentEngineService`;
  флаг дописан в `LOAD_TEST.md`.
- **В черновике доказательств вписал сравнение с августовскими цифрами по памяти** (неверное);
  заменил на реальные значения из `LOAD_TEST.md` до коммита.
- Локальные PostgreSQL и Redis дважды умирали посреди работы (не OOM) — перезапускал; из-за
  этого один integration-прогон пришлось начать заново.

## 5. Чем доказано (числа)

| Проверка | Результат |
| --- | --- |
| CI PR #67 (`dfd5a5a`) | 2 прогона × 5 jobs — 10/10 зелёные |
| API integration локально, RC | 125 suites, 1594/1594 |
| API unit / typecheck / eslint | 750/750 / 0 / 0 |
| Mobile jest / tsc / eslint | 78 suites 709/709 / 0 / 0 |
| Admin jest + node:test / tsc / eslint | 112/112 + 4/4 / 0 / 0 |
| Partner jest / tsc / eslint | 121/121 / 0 / 0 |
| `prisma migrate diff` (shadow-БД) | No difference |
| demo parity | diff пуст |
| Proxy probe | entries 2, marker 0, X-Real-IP pos 2 → hops 2 |
| Откат: smoke main-схема vs RC-схема | 19 ✓ / 20 ✗ в обоих, `diff` пуст |
| Нагрузка RC | capture 1835 req / 120.9 ok/s / p95 360 мс; replay 715 ok/s; payouts 82.2 ok/s; intents 53.6 ok/s; 0 failed; ledger 0.0000 / 21 569 postings |
| Backup/restore/verify | 1.0 с / 1.6 с / 3.0 с PASS, 79 миграций, счётчики = источник |
| Redis | graceful 5/1 → 5/1; crash 5/1 → 0/0 |
| `backup.sh` после фикса | 3/3 прогона ✓ |

## 6. UNVERIFIED

1. Всё, что требует телефона (32) и сторов (33).
2. Нагрузка и RPO/RTO на инфраструктуре Railway.
3. Денежный путь старым кодом на новой схеме (см. 4.2).
4. CI на `10d5d00` (docs + `backup.sh`) — прогон идёт после пуша; результат в PR #67.
5. Что RC-ветка после merge в `main` задеплоится без сюрпризов на Railway — не проверялось
   (нельзя без деплоя).

## 7. Кандидат-APK (пункт 31)

| Что | Значение |
| --- | --- |
| Workflow run | 35602999208 (`Build Android APK`, workflow_dispatch, ветка `claude/rc-20260921`) |
| Коммит | `dfd5a5ade3b4cfbea6c065a5c590fb84b1b06359` |
| Профиль / builder | `production-apk` (имя «TuTak», без пометок, sideload) / local (без EAS-кредита) |
| API | `https://tutak-api-production.up.railway.app/v1` (`DEMO_MODE` off) |
| Релиз | https://github.com/arman119090-cmyk/TuTak-Platform/releases/tag/apk-production-apk-68 — файл `tutak.apk` |
| Размер | 130 615 259 байт |
| SHA-256 | `c47c564da3e61dfed317638539d539c4fe928c133899129c0a09d921082ef51d` |
| Подпись | EAS-managed keystore проекта (та же, что у всех предыдущих сборок); отдельного upload key владельца нет — пункт 31 остаётся 🟡 по подписи |
| Artifact | `tutak-android-production-apk` (10641621695, zip 62 105 798 байт, хранится 30 дней) |

Это сборка кандидата, не релиз с `main`: `main` = `369eda1`, RC не влит. Она нужна для
пункта 32 (device review) — на ней есть всё из #60, #61, #63, #64, #65, включая новые экраны
аудита. Проверка MapTiler-ключа в workflow прошла (шаг «Ask the tile provider…»).

## 8. Вопросы владельцу

1. Дать поручение на merge #67 в `main` (после закрытия gates 7, 12, 32) — или сначала #60,
   потом остальное по одному? RC-ветка пригодна для обоих вариантов.
2. Разрешить `--appendonly yes --appendfsync everysec` в start command Redis на Railway
   (меняет production-сервис, делаю только с поручением).
3. Назначить людей: ежедневная сверка (п. 27), поддержка (п. 37), дежурство (п. 17).
4. Починить `scripts/smoke-test.sh` под права production-администратора — отдельная задача,
   если подтвердите (нужен, чтобы п. 6 работал).

## Дополнение

- 13:18Z: APK собран, см. раздел 7. Пункт 31 — кандидат выдан; статус 🟡 (подпись EAS-keystore, SHA не `main`).
- Итог по заданию: полностью закрыты 2, 9 (локально), 10, 14, 16; продвинуты 3, 4, 5 (RC + PR #67), 15, 27, 31, 37; остальное — владелец.
