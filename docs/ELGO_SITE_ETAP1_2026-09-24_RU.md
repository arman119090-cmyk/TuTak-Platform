# Отчёт: сайт ElGo, этап 1 (MVP) — 24.09.2026

## 1. Задание

ТЗ «сайт строительной компании ElGo» (полный текст сохранён в
`sites/elgo/BRIEF.md`). Коротко: премиальный сайт-визитка на Astro для
ереванской строительной компании ElGo. Этап 1 — длинная главная на hy/ru/en
(армянский по умолчанию, `/` → `/hy/`), политика конфиденциальности на трёх
языках, форма заявки `POST /api/lead` → Telegram Bot API с валидацией,
honeypot и лимитом по IP. Дизайн-токены и шрифты заданы (Cormorant Garamond +
Onest, для армянского — Noto Serif/Sans Armenian, self-host). Проекты —
Content Collections. SEO: title/description/OG/Twitter, hreflang, sitemap,
robots, JSON-LD `GeneralContractor`. Lighthouse mobile: Perf ≥ 90, A11y ≥ 95,
SEO ≥ 95. Деплой на Render/Railway + README с плейсхолдерами и чек-листом
«Перед запуском». Запрещено: выдумывать факты, публиковать банковский счёт,
неподтверждённые ИНН и лицензию, использовать фото без прав.

## 2. База

- Ветка: `claude/elgo-construction-site-mavmlm`
- Базовый коммит: `369eda1` (main, «Оповещения: «доставлено» — слово получателя…»)

## 3. Что сделано

Новый самостоятельный проект `sites/elgo/` (npm, свой lockfile; в
pnpm-воркспейс и CI TuTak не входит). В TuTak изменён один файл:
`eslint.config.mjs` — добавлен `sites/**` в ignores, чтобы корневой `pnpm lint`
не пытался линтовать чужой проект своим конфигом. Миграций нет.

- **Каркас**: Astro 7.3.4, `@astrojs/node` 11.1.6 (standalone),
  `@astrojs/sitemap`, `output: 'static'` — все страницы пререндерятся, серверные
  только `/api/lead` и `/` (настоящий 301 на `/hy/`; встроенный редирект i18n
  давал 302 — отключён).
- **i18n**: `src/i18n/{ru,hy,en}.json`, типизация по `ru.json` (нехватка ключа
  в hy/en ловится `astro check`). Армянский помечен черновиком (`_draft`).
- **Главная** — все 9 блоков из п. 6 ТЗ: `Header` (sticky, тёмный при скролле,
  бургер, иконка звонка, ՀՅ/RU/EN), `Hero` (полоса 01–04), `Directions`,
  `Process`, `Projects` (1 большая + 2 малые из `featured`), `Stats` (скрыта
  флагом), `About` (реквизиты без ИНН/лицензии), `Lead`, `Footer`,
  `Messenger` (выключен, пока не выбран мессенджер).
- **Переключатель языка сохраняет раздел**: к ссылке добавляется якорь
  секции, которая сейчас на экране.
- **Политика** `/{hy,ru,en}/privacy/` с плашкой «проверить юристом».
- **Форма** `src/lib/lead.ts` + `src/pages/api/lead.ts`: нормализация армянских
  номеров (+374 / 0XX / 00374), имя 2–80 символов (по символам, не байтам),
  тип из списка, согласие обязательно (и на клиенте, и на сервере), honeypot,
  лимит 5/10 мин на IP + 30/10 мин на весь сайт, ответ JSON без перезагрузки;
  без JS — HTML-страница с результатом на языке посетителя. Если токены
  Telegram не заданы — посетитель получает ошибку, а не ложный «успех».
- **Проекты**: `src/content.config.ts` (все поля ТЗ + `cover_alt`, `order`,
  `placeholder`), 3 файла-заглушки `placeholder-*.md` с «[Название объекта N]».
- **Изображения**: `astro:assets` `<Picture>` → AVIF/WebP, `srcset`, hero
  eager + `fetchpriority=high`, остальное lazy. Вместо фото — сгенерированные
  заглушки (плоские тона + линии, `scripts/make-placeholders.mjs`).
- **Шрифты**: `@fontsource/*` (self-host), `:lang(hy)` → Noto Armenian.
- **SEO**: title/description/OG/Twitter на каждом языке, canonical, hreflang
  + x-default, sitemap с альтернативами (без `/`), robots.txt, JSON-LD
  `GeneralContractor` (без ИНН и email, пока не подтверждены).
- **Бренд**: текстовый логотип ElGo (Cormorant 600) в контурах, favicon
  (E латунью на графите), apple-touch-icon, OG 1200×630, logo.png —
  `scripts/make-brand.py` + `make-brand-png.mjs`.
- **Деплой**: `sites/elgo/render.yaml` (Blueprint Path указывать вручную —
  в корне лежит render.yaml TuTak), `railway.json`, `.env.example`.
- **Документация**: `sites/elgo/README.md` — запуск, таблица «Что нужно от
  заказчика» (17 пунктов), чек-лист «Перед запуском», как поменять тексты,
  как добавить проект, как заменить фото, Render, Railway, Telegram-бот, домен.
- **Тесты**: `tests/lead.test.mjs` (юнит), `tests/browser-check.mjs`
  (Playwright: шрифты через CDP, адаптив, lazy-картинки, язык, меню, форма,
  клавиатура).

## 4. Что НЕ сделано

- ~~Сайт не задеплоен~~ — задеплоен после ответа владельца, см. раздел 8.
  Собственного домена нет (решение владельца), Telegram не подключён.
- **Доставка в Telegram не проверена вживую** — нет токена и chat_id. Проверено
  только: запрос в Bot API формируется правильно (юнит-тест с подменённым
  fetch) и режим `LEAD_DRY_RUN`.
- **Нет реальных фото** — все 9 изображений заглушки. Стоковые фото не брал:
  лицензию на них в рамках задачи подтвердить нечем.
- **Армянский и английский — перевод ИИ**, носителем не вычитаны.
- **«Все проекты →», страницы проектов, gallery** — этап 2, по ТЗ не делал;
  поле `gallery` в схеме есть, но нигде не выводится.
- **Сжатие ответов** (gzip/brotli) сервером Node не включено; Lighthouse
  отмечает «document latency» (~25 КБ экономии). Рассчитываю на сжатие у
  Render/Cloudflare — не проверено.
- **Корневой `pnpm lint` не прогнан** — в контейнере не установлены
  зависимости TuTak. Правка `eslint.config.mjs` — одна строка в `ignores`.
- **Собственные ошибки по ходу**:
  - Сначала написал в армянском словаре пометку «перевод не машинный» — это
    было неправдой (перевод сделан ИИ); исправил на честную.
  - В русских текстах сначала появились утверждения, которых нет в ТЗ
    («проектируем сами», «без цепочки субподрядчиков») — это выдуманные факты
    о компании; убрал. Осталась одна фраза-добавка, которую заказчик должен
    подтвердить: «реквизиты для оплаты выдаём только лично и по договору; не
    присылаем счета с других номеров» (вынесена в README).
  - Первая версия определения IP доверяла первому адресу X-Forwarded-For, а
    затем `X-Real-IP`/`CF-Connecting-IP` — всё это клиент может подделать и
    обойти лимит. Переделал: последний адрес XFF + настраиваемый доверенный
    заголовок + глобальный лимит как страховка.
  - Лимит сначала считал все попытки, включая невалидные, — человек с пятью
    опечатками блокировался бы. Перенёс после валидации.
  - Класс `.placeholder` сначала был с `opacity: .75` — на светлом фоне это
    давало контраст ~3.4:1 (< 4.5). Убрал.
  - `/` отдавал 302 (встроенный i18n-редирект), а не 301 — заменил своим.
  - В sitemap попадал `/` как дубль армянской страницы — отфильтровал.
  - Юнит-тесты сначала падали: Node strip-types не поддерживает parameter
    properties в конструкторе — переписал класс.
  - Два ложных провала в браузерной проверке были ошибками самого теста
    (плавная прокрутка не успевала; проверка шрифта смотрела на `<dl>` без
    текста) — исправил тест, не код.

## 5. Чем доказано

- `astro check`: 26 файлов, **0 errors, 0 warnings, 0 hints**.
- `npm test`: **13 тестов, 13 pass, 0 fail** (телефоны: 7 валидных / 9
  невалидных форматов, имя, тип, согласие, honeypot, лимит, IP, текст
  сообщения, запрос в Bot API и обработка ошибки API).
- `astro build`: успешно, 7 HTML-страниц + сервер.
- curl по собранному серверу: `/` → **301** `/hy/`; `/hy/ /ru/ /en/
  /hy/privacy/ /robots.txt /sitemap-index.xml /og.png` → 200; `/nope` → 404;
  `GET /api/lead` → 405; валидная заявка → 200; короткое имя / номер +7 /
  неизвестный тип / без согласия → 400 с нужным кодом; honeypot → 200 без
  отправки; 6-я валидная заявка с одного IP → **429**; другой IP проходит;
  отправка формы без JS → HTML-ответ на армянском.
- `tests/browser-check.mjs` (Chromium 1194): **42 проверки OK, 0 FAIL**:
  - фактически отрисованные шрифты (CDP `getPlatformFontsForNode`): hy — h1
    Noto Serif Armenian, текст Noto Sans Armenian, системных нет; ru/en —
    Cormorant Garamond + Onest; армянское «ԷԼԳՕ» на RU-странице — Noto Sans
    Armenian;
  - 390 / 768 / 1280 / 1440 × hy/ru/en — без горизонтального скролла, все
    картинки загружены (12 + 12 проверок);
  - RU → EN из «О компании» → `/en/#about`;
  - бургер открывает, Escape закрывает, у иконки звонка есть aria-label;
  - без согласия запрос не уходит, с согласием — «Thank you!…» без
    перезагрузки; первый Tab — skip-link.
- Lighthouse 13.5.0, мобильный профиль, локальный сервер:

  | Страница | Perf | A11y | Best | SEO | LCP | CLS |
  |---|---|---|---|---|---|---|
  | /hy/ | 97 | 100 | 100 | 100 | 2.3 s | 0.034 |
  | /ru/ | 97 | 100 | 100 | 100 | 2.2 s | 0.001 |
  | /en/ | 99 | 100 | 100 | 100 | 1.7 s | 0.018 |
  | /hy/privacy/ | 98 | 100 | 100 | 100 | 1.8 s | 0.023 |
  | /ru/privacy/ | 99 | 100 | 100 | 100 | 1.7 s | 0 |
  | /en/privacy/ | 98 | 100 | 100 | 100 | 1.8 s | 0.005 |

- Скриншоты всех разрешений просмотрены глазами (hy/ru 1440, hy 390, меню).

## 6. UNVERIFIED — что осталось непроверенным

- Реальная доставка заявки в Telegram-чат.
- Работа на Render/Railway: сборка на их Node, `render.yaml`/`railway.json`,
  healthcheck, сжатие, HTTPS, домен.
- Какой адрес на Render/Railway оказывается последним в `X-Forwarded-For`
  (от этого зависит, что лимит считается по реальному IP клиента, а не по
  IP прокси — тогда 5 заявок на всех). Может понадобиться `CLIENT_IP_HEADER`.
- Lighthouse с реальными фото (заглушки весят 1–40 КБ, реальные фото будут
  тяжелее — Performance может просесть).
- Lighthouse на реальном хостинге, а не на localhost.
- Качество армянского и английского текстов.
- Safari/iOS и Firefox — проверялся только Chromium.
- Поведение на реальном телефоне (звонок по `tel:`, клавиатура в форме).
- Корневой `pnpm lint` TuTak после правки `eslint.config.mjs`.

## 7. Вопросы владельцу

1. **Где жить проекту?** Сейчас он в `sites/elgo/` репозитория TuTak только
   потому, что ветка задачи — здесь. С TuTak он не связан; логичнее отдельный
   репозиторий `elgo-site`. Перенести?
2. **Деплоить?** Render или Railway, какой тариф (free засыпает — первая
   заявка после простоя ждёт 30–50 с)? Нужен ли мой деплой через подключённые
   Render/Railway, или создадите сервис сами по README?
3. **Telegram**: кто создаёт бота и в какой чат слать заявки?
4. **Мессенджер в углу**: WhatsApp или Telegram, и какой номер/username?
5. Подтвердить фразу о платёжных реквизитах в «О компании» или убрать.
6. Английское написание имени директора (сейчас «Arsen Iskandaryan») и
   английский адрес.
7. Остальное — по таблице «Что нужно от заказчика» в `sites/elgo/README.md`
   (фото, тексты, ՀՎՀՀ, лицензия, гарантия, часы, домен, почта, цифры).

## 8. Дополнение: деплой (24.09.2026, после ответа владельца)

**Задание:** «домен пока ещё пусть будет бесплатным» — то есть разворачиваем
на бесплатном поддомене платформы, без своего домена.

**Что сделано**
- Через подключённый Render создан сервис `elgo-site` (id
  `srv-daqee3o473hc73frr190`) в «Arman's workspace»: тариф free, Frankfurt
  (как остальные демо в этом аккаунте), runtime Node, ветка
  `claude/elgo-construction-site-mavmlm`, автодеплой по коммиту.
- Адрес: **https://elgo-site.onrender.com** (`/` → `/hy/`).
- Build: `cd sites/elgo && npm ci && npm run build`, Start:
  `cd sites/elgo && npm start` (инструмент создания сервиса не принимает
  Root Directory, поэтому `cd`). Переменные: `NODE_VERSION=22.22.2`,
  `HOST=0.0.0.0`, `SITE_URL=https://elgo-site.onrender.com`.
- README: добавлен раздел «Где сайт сейчас».

**Чем доказано**
- Деплой `dep-daqee4g473hc73frr2k0` на коммите `65697c1`: статус **live**,
  сборка 09:02:10 → 09:03:11 UTC.
- Логи Render: `astro build` прошёл, `Server listening`, «Your service is live».

**Что НЕ сделано / UNVERIFIED**
- **Сайт снаружи не открыт ни разу мной.** Сетевой прокси этой сессии
  блокирует `*.onrender.com` (curl: `CONNECT tunnel failed, response 403`;
  WebFetch: `EGRESS_BLOCKED`). Статус live означает, что Render достучался до
  порта сервиса, но страницы глазами на проде я не видел. Нужно открыть адрес
  в браузере.
- Healthcheck-путь не задан (инструмент создания его не принимает); Render
  проверяет только открытый порт. Можно поставить `/hy/` в панели:
  Settings → Health Check Path.
- Форма на проде отвечает ошибкой «не удалось отправить, позвоните» —
  `TELEGRAM_BOT_TOKEN` / `TELEGRAM_CHAT_ID` не заданы.
- Сайт на поддомене индексируется поисковиками (canonical указывает на
  onrender.com). Когда появится домен, `SITE_URL` поменять — canonical
  переедет. Если индексировать временный адрес не нужно — скажите, добавлю
  noindex-флаг.
- Free-тариф засыпает после ~15 минут простоя, первое открытие — 30–50 с.

**Вопросы владельцу**
1. Откройте https://elgo-site.onrender.com — всё ли отображается?
2. Telegram-бот: создадите сами (инструкция в README) или помочь?

## 9. Дополнение: деплой на Railway (24.09.2026)

**Задание:** «Ты не можешь сделать сайт на рейловей?» — развернуть на Railway.

**Что сделано**
- Через подключённый Railway создан проект `elgo-site` (workspace
  «arman119090-cmyk's Projects»), сервис `elgo-site` из репозитория, ветка
  `claude/elgo-construction-site-mavmlm`, Root Directory `/sites/elgo`,
  Watch Paths `/sites/elgo/**`, Build `npm run build`, Start `npm start`,
  Healthcheck `/hy/`, restart ON_FAILURE, переменные `HOST`, `SITE_URL`,
  `RAILPACK_NODE_VERSION=22`.
- Бесплатный домен: **https://elgo-site-production.up.railway.app**.
- `sites/elgo/railway.json` удалён: Railway отверг его как устаревший
  (Config as Code deprecated). README переписан: раздел «Где сайт сейчас»
  и «Деплой на Railway» — настройки через панель.

**Чем доказано**
- Деплой `d5f005f7-a507-43b8-93ad-090c979960ed` на коммите `22ebd66`:
  статус **SUCCESS**; в логах `Server listening … :8080` и
  `[1/1] Healthcheck succeeded!` по пути `/hy/` — то есть Railway получил
  200 от армянской главной.

**Что НЕ сделано / ошибки по ходу**
- **Первые две сборки упали — обе по моей вине:**
  1. `create-deployment` взял ветку `main`, хотя я передал нашу ветку, —
     собрался корень TuTak. Надо было сразу проверить источник; перевёл
     сервис на нужную ветку через `connect-service-source`.
  2. В команде сборки был `npm ci && npm run build` (перенёс из render.yaml).
     Railpack сам ставит зависимости и монтирует кэш в
     `node_modules/.astro` — `npm ci` падает с `EBUSY`. Исправил на
     `npm run build`.
- **Регион не перенесён.** Railway по умолчанию поставил US West (sfo).
  Перенос в EU West (Amsterdam) подготовлен агентом Railway (staged), но
  `accept-deploy` дважды упал по таймауту 60 с. Изменение висит в панели —
  нужно нажать «Apply changes» (или «Deploy») в проекте `elgo-site`. Пока сайт
  отдаётся из США: для Армении это лишние ~150–200 мс на запрос.
- Снаружи сайт мной не открыт: `*.up.railway.app` заблокирован сетевой
  политикой сессии так же, как onrender.com. Подтверждение — только
  healthcheck самого Railway.
- Render-сервис `elgo-site` оставлен работать — удалять без вашего слова не
  стал. Два хостинга одного сайта — это два адреса в поиске; лишний стоит
  удалить.
- Бесплатный план Railway ограничен кредитами; если их исчерпать, сервис
  остановится. Лимиты аккаунта я не смотрел — UNVERIFIED.

**Вопросы владельцу**
1. Нажмите «Apply changes» в Railway — или разрешите мне повторить позже.
2. Удалить сервис на Render (`elgo-site.onrender.com`)?
