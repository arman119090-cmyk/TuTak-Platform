# Что осталось недоделанным — по пунктам

Состояние на 2026-09-01. HEAD = `91e7b64`, всё запушено, незакоммиченного нет.
Порядок: от блокирующего к необязательному.

## 1. E2E-тест партнёрского входа (блокирует зелёный CI)

Единственный красный шаг после правки `demo/`. Вход в дашборд партнёра
остаётся на `/login`.

```
[setup] › tests/e2e/auth.setup.ts:64:6 › sign in to both dashboards
Error: expect(page).not.toHaveURL(expected) failed
Expected pattern: not /\/login$/
Received string: "http://localhost:3001/login"
1 failed, 12 did not run
```

- Поломка **давняя** — CI красный минимум с 30 августа, 30 прогонов подряд,
  включая SHA `1861e14`, названный в аудите ChatGPT.
- **Блокер:** в рабочем контейнере нет Docker-демона, стек не поднять,
  Playwright не прогнать.
- **От оператора:** прогон у себя
  `docker compose up -d && pnpm exec playwright test tests/e2e/auth.setup.ts`,
  либо `error-context.md` и скриншоты из артефакта `playwright-report`
  последнего прогона — там точное сообщение на странице.

Чинить вслепую нельзя: правка логики входа без возможности проверить —
недопустимо перед запуском в production.

## 2. Деплой staging и живая проверка OTP

Кандидат `5d86ff8` не задеплоен и не проверен.

- **Блокер:** egress к `onrender.com` запрещён политикой окружения
  (403 на CONNECT), креденшелов Render нет.
- **От оператора:** задать переменные (п. 3), задеплоить, подтвердить.
  После этого проверяется живой OTP-поток.

## 3. Переменные окружения, которые может дать только оператор

- `SMS_ENDPOINT` + учётные данные — **без него отправка кода отдаёт 503**
- `SMS_GLOBAL_MAX_PER_HOUR`, `SMS_GLOBAL_MAX_PER_DAY`
- `CORS_ORIGINS`

## 4. Измерение хопов для клиентского IP

`CLIENT_IP_STRATEGY` и `CLIENT_IP_TRUSTED_HOPS` **не заданы намеренно**.
Пока не заданы — лимиты по IP отключены, расходы держит глобальный бюджет SMS.

Значение нужно **измерить** на живом сервисе (процедура в
`docs/RENDER_STAGING_RU.md`), а не угадать: завышение на единицу выбирает
адрес, который написал атакующий.

## 5. Обновление зависимостей (6 из 7 исправимы)

Не сделано сознательно: правка идёт через `pnpm.overrides`, меняет lockfile,
а lockfile участвует в Docker-сборке (`--frozen-lockfile`) и во всех прогонах
тестов. Ломать зелёные шаги ради advisory-шага, который и так зелёный, —
неверный размен.

Исправимые: `nanoid` (≥3.3.18), `js-yaml` (≥5.2.2), `deepmerge-ts` (≥8.0.0),
`uuid` (≥11.1.1), `decode-uri-component` (≥0.5.0).

Делается отдельным изменением с полным прогоном тестов — по команде.

## 6. `image-size` — исправления не существует

Две High-уязвимости, `patched: <0.0.0`. Dev-зависимость metro-бандлера Expo,
в рантайм не попадает. Ждать апстрим.

## 7. Railway против Render — расхождение, требующее решения

Следующим шагом заявлен запуск на **Railway**, но вся выполненная работа
целилась в **Render**: `render.yaml`, процедура измерения хопов, документация.
Один-в-один не переносится:

- `render.yaml` для Railway бесполезен — нужен свой конфиг;
- **число доверенных хопов у Railway другое**, измерять заново;
- поведение прокси с `X-Forwarded-For` у платформ различается.

Нужно решить целевую платформу, иначе staging и production разойдутся в
предположениях о клиентском IP.

## 8. Открытый вопрос по бизнес-логике (задан, ответа не было)

При branch-scoping сотрудник, привязанный к филиалу, **не видит операции без
филиала** — устаревший партнёрский QR, EV-зарядки, роуминг. Выбрано безопасное
поведение (скрывать). Следствие: кассир филиала не увидит EV-операций своего
партнёра вообще. Если нужно иначе — требуется решение владельца.

## 9. Мелочи, не блокирующие

- **Суточного лимита OTP на телефон нет** — только часовой (5/час, то есть до
  120/сутки при непрерывной атаке).
- **`verify-phone/request` и `password-reset/request`** — часового лимита по IP
  у них нет, только burst-троттлинг; глобальный бюджет SMS их покрывает.
- **23 ГБ WAL-архива** в контейнере — по указанию владельца не трогается.

## Предлагаемый порядок

п. 7 (решить платформу) → п. 3 (переменные) → п. 2 (деплой) → п. 1 (e2e с
прогоном оператора) → п. 5 (зависимости отдельно).
