# Отчёт: запуск Little Joe на Railway

## 1. Задание

Владелец написал: «Ну давай подключи к Railway, чтобы начинать работу там же».
Затем уточнил: Railway, к которому я подключён, — это другой аккаунт (тот,
где TuTak). Для Little Joe нужен его новый аккаунт Railway.

## 2. База

Ветка `claude/little-joe-armenia-ecommerce-ccikt1`, от коммита `74f413d`.

## 3. Что сделано

- Код, коммиты `165667e` и `5d3d374`:
  - `little-joe-armenia/Dockerfile.railway` — образ без встроенной базы. База
    отдельная, на постоянном диске Railway. При старте выполняются
    `migrate deploy`, затем сид, затем `next start`.
  - `src/lib/env.ts` — если `APP_URL` не задан, адрес берётся из
    `RAILWAY_PUBLIC_DOMAIN`.
- На Railway, в аккаунте **styop0909** (том же, где TuTak):
  - проект `little-joe-armenia`;
  - сервис Postgres (шаблон `postgres`, образ postgres-ssl:18);
  - сервис `web`, собранный из ветки выше:
    - rootDirectory `/little-joe-armenia`;
    - healthcheck `/api/health`;
    - сон выключен;
    - демо-режим (`DEMO_MODE`, mock-оплата);
  - домен https://web-production-4be52.up.railway.app

## 4. Что НЕ сделано

- **Моя ошибка: проект создан не в том аккаунте.** Я подключился к тому
  Railway, что был привязан к сессии, — styop0909 с проектами TuTak и TuTak
  Bot. Не спросил, тот ли это аккаунт. Это прямо противоречит исходному
  требованию «чтобы не ассоциировалось с TuTak». Проект нужно перенести в
  новый аккаунт владельца.
- Проект в styop0909 **не удалён**. Удалять без согласия владельца нельзя.
  Пока он работает, он расходует кредит этого аккаунта.
- Подключиться к новому аккаунту Railway сам я не могу. Коннектор Railway в
  claude.ai переподключает владелец, после этого нужна новая сессия.
- Сначала я добавил `railway.json`. Railway отклонил его как устаревший
  формат, и файл пришлось удалить (коммит `5d3d374`).

## 5. Чем доказано

- Юнит-тесты: 176 из 176 прошли. `tsc --noEmit` — без ошибок. Линт: 0
  ошибок, 1 предупреждение — оно было и до моих изменений.
- Деплой `8e0d3ae2` на Railway в статусе SUCCESS, healthcheck пройден.
- В логах деплоя видно:
  - применены 2 миграции;
  - `[seed] admin owner@example.com created`;
  - `Next.js 16.3.6 Ready`.

## 6. UNVERIFIED

- Открыть сайт по публичному адресу из моего окружения не удалось: сетевая
  политика среды не пускает к `*.up.railway.app`. Что сайт работает, видно
  только по статусу деплоя и логам.
- Вход в админку и загрузка фото на Railway не проверены.

## 7. Вопросы владельцу

1. Удалить проект `little-joe-armenia` из аккаунта styop0909 после переноса
   в новый аккаунт? Или сразу?
2. Новый аккаунт Railway уже на тарифе Hobby ($5/мес)? На Free приложению
   вместе с базой не хватит лимитов.

---

## 8. Дополнение: перенос в собственный аккаунт владельца

### 8.1. Задание

«Там другой Railway, это не мне принадлежит. Создай новый Railway для меня и
подключись к нему». Владелец зарегистрировался в Railway и переподключил
коннектор.

### 8.2. Что сделано

- Проверил, чей аккаунт теперь подключён: `whoami` вернул **arman119090-cmyk**,
  воркспейс «arman119090-cmyk's Projects». Проектов в нём не было.
- Создал проект `little-joe-armenia` (id `7685ffd8…`) и в нём:
  - Postgres из шаблона `postgres`;
  - сервис `web` с теми же настройками, что в разделе 3.
- Сгенерировал новые секреты и новый пароль администратора. Пароль сообщён
  владельцу в чате, в файлы не записан.
- Сайт доступен по адресу https://web-production-2354b.up.railway.app

### 8.3. Чем доказано

- Деплой `8d3c43f0` (коммит `6bbee2b`) в статусе SUCCESS, healthcheck
  `/api/health` пройден.
- В логах деплоя: миграции применены, `admin owner@example.com created`,
  `Next.js Ready`.

### 8.4. Что НЕ сделано

- Проект в **чужом** аккаунте styop0909 **не удалён**:
  - адрес web-production-4be52.up.railway.app;
  - после переподключения коннектора у меня нет доступа к тому аккаунту.

  Удалить его может только владелец аккаунта styop0909: Project → Settings →
  Danger → Delete. В переменных того проекта лежит старый пароль админки
  (`LJ-kcST…`), к новому сайту он не подходит.
- Открыть сайт из моей среды по-прежнему нельзя: сетевая политика среды
  запрещает доступ. Поэтому вход в админку и загрузку фото вживую я не
  проверил.
- Тариф аккаунта через API не виден. По словам владельца, тариф платный.

### 8.5. UNVERIFIED

- Страницы магазина и вход в админку по новому адресу.
- Расход денег за месяц на новом аккаунте.
