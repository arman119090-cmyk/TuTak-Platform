# Отчёт: мессенджеры как основной контакт и «Deer Hunt» — 24.09.2026

## 1. Задание

1. «033228733 — добавь этот контакт как основной Viber, WhatsApp, Telegram и
   сделай так, чтобы всё было очень красиво и классически».
2. К скриншоту Instagram с работой Delabrière: «надо написать не Hunter
   dogs, а Deer hunt» (текст сообщения распознан голосом; понял как
   переименование работы).

## 2. База

- Ветка: `claude/new-session-xjpidi`
- Базовый коммит: `7a56a1d`

## 3. Что сделано

### Контакт
- Номер `033 228 733` принят как армянский мобильный и записан в
  международном формате **+374 33 228 733**:
  - WhatsApp — `https://wa.me/37433228733`, открывается с готовым
    сообщением на языке страницы (на карточке — с названием работы,
    например «Здравствуйте! Меня интересует работа «Aknuni / Ակնունի» из
    коллекции LEVANI ART.»);
  - Viber — `viber://chat?number=%2B37433228733`;
  - Telegram — `https://t.me/+37433228733`.
- **Оформление:** три одинаковые плашки в одну строку: тонкая бронзовая
  рамка, внутренняя рамка-паспарту, линейные иконки в цвет бронзы (без
  фирменных зелёного и фиолетового), название капителью; под ними номер
  антиквой Cormorant с выровненными (lining) цифрами. На hover — тёплая
  подсветка рамки.
- **Где:**
  - на всех 18 карточках вместо кнопки Instagram (блок «Запросить об этой
    работе» → плашки → номер → «укажите название работы» → «Также в
    Instagram»);
  - на странице запроса (заголовок «Напишите нам», плашки, номер,
    Instagram);
  - в футере, в колонке «Контакты» (компактные плашки и номер);
  - в диалоге «View in an Interior» (кнопка «Запрос в WhatsApp» с
    названием работы).
- Instagram везде остался второстепенной ссылкой.
- Privacy Notice (6 языков): упомянуты WhatsApp, Viber, Telegram и
  Instagram — сообщения в них обрабатывает сам сервис.
- В JSON-LD ArtGallery добавлен `telephone`.
- Звонок (`tel:`) не добавлял: просили мессенджеры.

### Название работы
- `edouard-delabriere-hunter`: «Hunter with Dogs Sculpture» → **«Deer
  Hunt»** (главная — подпись hero, коллекция, карточка, поиск, alt, SEO).
  Адрес страницы не менялся.

### Файлы
`src/content/site.ts`, `src/content/catalog.source.json`,
`src/components/ContactChannels.tsx` (новый), `Footer.tsx`,
`app/[locale]/artworks/[slug]/page.tsx`, `app/[locale]/enquire/page.tsx`,
`app/[locale]/page.tsx`, `app/globals.css`, словари 6 языков, тесты e2e и
smoke, `README.md`, `CONTENT_TODO.md`, скриншоты
`docs/screenshots/contacts-*.jpg`.

## 4. Что НЕ сделано / допущения

- **Формат номера — допущение.** «033228733» прочитан как армянский
  +374 33 228 733. Если номер другой страны, это правка одной строки в
  `site.ts`.
- **Telegram по номеру** откроет чат, только если в настройках Telegram
  владельца разрешено находить его по номеру. Надёжнее ссылка по
  username — если он есть, пришлите.
- **Viber** открывается только там, где установлено приложение (схема
  `viber://`); на компьютере без Viber кнопка ничего не сделает.
- Реальное открытие чатов на телефоне не проверялось; проверены адреса
  ссылок и текст сообщения WhatsApp.
- **Написание фамилии.** На скриншоте из Instagram — «Delabrièrre»
  (двойная r), на сайте из каталога пакета — «Delabrière». Не менял;
  вопрос ниже.
- **Собственная ошибка:** при проверке теста подписей не ограничил поиск
  блоком карточки — ссылка «Написать нам в Telegram» есть и в футере, тест
  упал на двойном совпадении. Исправлен тест.

## 5. Чем доказано

Локально: typecheck 0, lint 0, vitest 27/27, e2e 64/64, smoke-набор против
локальной статической сборки 58/58.

Production — раздел 6.

## 6. Production

- **URL:** https://levani-art.onrender.com
- **Коммит с изменениями:** `d5206907915936fb51b0270990e03da13e3022f7`; следом
  `e8f2ea25` — только правка smoke-теста, сайт тот же. Render:
  `dep-daqm6jnf3r2c73abp6vg` (`e8f2ea2`) — live с 17:53:16 UTC.
- **Smoke на публичном URL, первый прогон** (run 36036543366, `d520690`):
  **57/58.** Упала одна проверка: на hy/desktop фото одной из 18 карточек
  пришло с `naturalWidth = 0`; какая именно работа, тест не сообщал. Те же
  18 фото прошли в остальных 11 комбинациях язык × экран. Тест доработан:
  теперь ждёт загрузку и называет работу и файл.
- **Второй прогон** (run 36037295298, `e8f2ea2`, старт 17:54:47, после
  live): **58/58 за 53.6 с.** Проверены:
  - на каждой из 18 карточек × 6 языков × 2 экрана — ссылки WhatsApp
    (сообщение содержит название этой работы), Viber, Telegram, видимый
    номер +374 33 228 733, Instagram;
  - то же в футере и на /enquire;
  - «Deer Hunt» на карточке и в подписи hero, «Hunter with Dogs» на
    странице нет;
  - все прежние проверки: языки, гербы, изображения, sitemap, битые
    ссылки, 404, отсутствие формы и цен.

**UNVERIFIED:** причина единичного незагруженного фото в первом прогоне не
установлена. Повтор не воспроизвёл сбой; если он повторится, тест теперь
назовёт файл.

## 7. Вопросы владельцу

1. Фамилия автора «Deer Hunt»: оставить «Édouard Delabrière» или писать
   как в Instagram — «Delabrièrre»?
2. Есть ли у владельца username в Telegram? Ссылка по username надёжнее
   ссылки по номеру.
