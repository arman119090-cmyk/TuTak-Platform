# Статус проекта TuTak в Railway — 09.09.2026

## Подключение

Railway-коннектор в чате виден и работает.

- Аккаунт: **styop0909** (stepan.grig.2009@gmail.com)
- Workspace: **styop0909's Projects**
- Проект: **TuTak** (`901cf202-7c6a-4403-8853-56c36d684a53`)
- Окружение: **production** (`aebe04da-ba8f-44b5-9a7f-3d4d43284966`)

## Сервисы и деплой

| Сервис | Образ | Реплики | Регион | Статус деплоя | Дата деплоя |
|---|---|---|---|---|---|
| Postgres | `ghcr.io/railwayapp-templates/postgres-ssl:18` | 1 | sfo | ✅ SUCCESS | 01.09.2026 20:24 |
| Redis | `redis:8.2` | 1 | sfo | ✅ SUCCESS | 01.09.2026 20:31 |

Оба сервиса живые, последние деплои прошли успешно, других деплоев за это время не было.

## Тома (volumes)

| Том | Сервис | Точка монтирования | Размер |
|---|---|---|---|
| postgres-volume | Postgres | `/var/lib/postgresql/data` | 5000 MB |
| redis-volume | Redis | `/data` | 5000 MB |

## ⚠️ Незадеплоенные (staged) изменения

В окружении **production** есть незакоммиченный патч (24 изменения, создан 01.09.2026, последний раз обновлён 09.09.2026 19:22), который **ещё не применён**. Railway помечает его как не деструктивный, но по сути он:

1. **Удаляет** у сервиса **Postgres** переменные окружения: `DATABASE_URL`, `PGDATA`, `PGDATABASE`, `PGHOST`, `PGPASSWORD`, `PGPORT`, `PGUSER`, `POSTGRES_DB`, `POSTGRES_PASSWORD`, `POSTGRES_USER`, `RAILWAY_DEPLOYMENT_DRAINING_SECONDS`, `SSL_CERT_DAYS`.
2. **Удаляет** у сервиса **Redis** переменные: `REDISHOST`, `REDISPASSWORD`, `REDISPORT`, `REDISUSER`, `REDIS_PASSWORD`, `REDIS_URL`.
3. **Добавляет** алерты по использованию места (80% / 95% / 100%) на оба тома (`redis-volume`, `postgres-volume`).

Пункты 1 и 2 выглядят подозрительно: если приложение подключается к базе/Redis через эти переменные, применение патча (`accept-deploy`) может **разорвать подключение** других сервисов к Postgres/Redis. Это стоит проверить перед принятием патча — возможно, переменные переехали в другое место (shared variables) или патч устарел.

Патч пока не применялся, я ничего не коммитил и не деплоил — только прочитал текущее состояние.

## Рекомендация

Прежде чем нажимать "accept-deploy" по этому staged-патчу, стоит явно решить, что делать с удалением переменных подключения к Postgres/Redis — иначе после применения патча другие сервисы (если они читают эти переменные) могут перестать подключаться к базе и кэшу.
