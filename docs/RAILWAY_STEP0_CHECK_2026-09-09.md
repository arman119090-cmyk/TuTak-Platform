# Шаг 0 — проверка состояния Railway перед деплоем API (только чтение)

Дата проверки: 09.09.2026. Проект `TuTak` (`901cf202-7c6a-4403-8853-56c36d684a53`),
окружение `production` (`aebe04da-ba8f-44b5-9a7f-3d4d43284966`). Ничего не
создавалось и не менялось — только чтение через Railway MCP.

## Главный результат: ⚠️ старый патч НЕ отменён

Ожидалось, что владелец отменил (Discard) отложенный патч на 70 изменений.
По факту в окружении `production` всё ещё висит **staged-патч**:

| поле | значение |
|---|---|
| `patchId` | `e0b2070b-341d-4fdb-9590-a3d4e55c78ee` |
| статус | `STAGED` (не применён, не отменён) |
| количество изменений | **24** |
| создан | 01.09.2026 20:18 |
| последнее обновление | 09.09.2026 19:22 |

Это тот же `patchId` и то же число изменений (24), что были зафиксированы в
прошлой проверке этого чата — то есть патч не отменялся и не изменялся между
проверками. Число 24 отличается от упомянутых в задаче «70 изменений» — либо
формулировка «70» относилась к другому моменту/подсчёту, либо часть патча уже
была снята ранее. В любом случае **патч по-прежнему активен**, а не удалён.

### Что содержит патч (без изменений с прошлой проверки)

1. **Postgres** (`34eca4f9-3a7e-44a0-bb57-fb4e948dd17c`) — удаление 12
   переменных: `DATABASE_URL`, `PGDATA`, `PGDATABASE`, `PGHOST`, `PGPASSWORD`,
   `PGPORT`, `PGUSER`, `POSTGRES_DB`, `POSTGRES_PASSWORD`, `POSTGRES_USER`,
   `RAILWAY_DEPLOYMENT_DRAINING_SECONDS`, `SSL_CERT_DAYS`.
2. **Redis** (`a3f7eb92-0834-4d34-8b01-9564c7f03b45`) — удаление 6 переменных:
   `REDISHOST`, `REDISPASSWORD`, `REDISPORT`, `REDISUSER`, `REDIS_PASSWORD`,
   `REDIS_URL`.
3. **Тома** `postgres-volume` и `redis-volume` — добавление алертов по
   заполнению (80% / 95% / 100%), это безобидная часть патча.

Railway помечает патч как `destructive: false`, но по содержанию пункты 1–2
удаляют переменные подключения, которые может использовать `DATABASE_URL` /
`REDIS_URL` ссылками будущий сервис API (`${{Postgres.DATABASE_URL}}`,
`${{Redis.REDIS_URL}}` из плана деплоя). Если этот патч применить — эти
ссылки перестанут резолвиться.

## Текущие (live) переменные — не тронуты

Прямой запрос списка переменных подтверждает: пока патч не применён, все
исходные переменные обоих сервисов физически присутствуют:

- **Postgres**: `DATABASE_URL`, `PGDATA`, `PGDATABASE`, `PGHOST`, `PGPASSWORD`,
  `PGPORT`, `PGUSER`, `POSTGRES_DB`, `POSTGRES_PASSWORD`, `POSTGRES_USER`,
  `RAILWAY_DEPLOYMENT_DRAINING_SECONDS`, `SSL_CERT_DAYS` — присутствуют, плюс
  системные `RAILWAY_*`.
- **Redis**: `REDISHOST`, `REDISPASSWORD`, `REDISPORT`, `REDISUSER`,
  `REDIS_PASSWORD`, `REDIS_URL` — присутствуют, плюс системные `RAILWAY_*`.

Значения не запрашивались и в отчёт не выводятся.

## Сервисы и деплой — без изменений

| сервис | id | состояние | последний деплой | том |
|---|---|---|---|---|
| Postgres | `34eca4f9-3a7e-44a0-bb57-fb4e948dd17c` | live-with-staged-changes | SUCCESS, 01.09.2026 20:24 | postgres-volume, 5000 MB, цел |
| Redis | `a3f7eb92-0834-4d34-8b01-9564c7f03b45` | live-with-staged-changes | SUCCESS, 01.09.2026 20:31 | redis-volume, 5000 MB, цел |
| API (`tutak-api`) | — | **не существует** | — | — |

Оба сервиса живые и работают на деплоях от 1 сентября, других деплоев не
было. Сервис API ещё не создавался — подтверждено списком сервисов проекта.

## Вывод и что дальше

**Условие шага 0 не выполнено**: патч не отменён, он по-прежнему в статусе
`STAGED`. Postgres и Redis сами по себе целы (данные, тома, деплой не
пострадали — патч ещё не применён), но риск остаётся: если кто-то (человек
или процесс) нажмёт "Deploy changes"/`accept-deploy` в Railway, переменные
подключения к Postgres и Redis будут удалены.

Ничего не создаю и не применяю до вашего решения. Нужно явное подтверждение
по одному из двух путей:

1. **Отменить патч сейчас** (Discard) — тогда шаг 0 закрыт, можно переходить
   к шагу 1 (проверка чистоты базы) и шагу 2 (создание сервиса `tutak-api`).
2. Либо подтвердить, что патч специально оставлен staged и трогать его пока
   не нужно — тогда явно зафиксировать, что `accept-deploy` в проекте `TuTak`
   применять нельзя ни по какому поводу, пока патч не разобран.

До ответа шаг 1 (проверка `_prisma_migrations`) и шаг 2 (создание сервиса) не
начинаю.
