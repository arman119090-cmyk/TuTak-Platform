# Создание сервиса tutak-api — 09.09.2026

## Что сделано

В проекте `TuTak` (`901cf202-7c6a-4403-8853-56c36d684a53`), окружение
`production` (`aebe04da-ba8f-44b5-9a7f-3d4d43284966`), создан сервис:

| поле | значение |
|---|---|
| Название | `tutak-api` |
| id сервиса | `39cf03f4-bffe-4d77-a485-44ec9bab412b` |
| Репозиторий | `arman119090-cmyk/TuTak-Platform` |
| Ветка | `deploy/railway-api-df1fd99` |
| Коммит, который собирается | `df1fd996eaf089e91f10d5f6e3698d280a6f5794` — совпадает с зафиксированным SHA |
| Домен | `https://tutak-api-production.up.railway.app` (порт 4000) |

Порядок действий: сначала создан пустой сервис → сгенерирован домен →
заданы переменные → и только затем подключена ветка (это и запускает первый
билд/деплой — иначе источник не подключить).

## Заданные переменные

```
NODE_ENV=production
APP_ENV=staging
DEMO_MODE=true
PORT=4000
DATABASE_URL=${{Postgres.DATABASE_URL}}
REDIS_URL=${{Redis.REDIS_URL}}
QUEUE_PREFIX=tutak-test
CORS_ORIGINS=https://${{RAILWAY_PUBLIC_DOMAIN}}
MEDIA_PUBLIC_BASE_URL=https://${{RAILWAY_PUBLIC_DOMAIN}}
MEDIA_STORAGE_DRIVER=local
SMS_GLOBAL_MAX_PER_HOUR=50
SMS_GLOBAL_MAX_PER_DAY=200
SEED_BASELINE=true
```

`DATABASE_URL` и `REDIS_URL` — ссылки на существующие сервисы `Postgres` и
`Redis` в этом же проекте, как и требовалось.

## ⚠️ Не задано намеренно — нужно от вас

Три секрета я не вводил (по вашим же правилам — они вводятся только в
панели Railway, не через чат, не в git):

- `JWT_ACCESS_SECRET` (≥32 символов)
- `JWT_REFRESH_SECRET` (≥32 символов, другой)
- `SEED_ADMIN_PASSWORD` (≥12 символов)

**Без них первый билд, скорее всего, соберётся, но контейнер упадёт при
старте** — строгая проверка JWT-секретов привязана к `NODE_ENV=production` и
сейчас сработает. Добавьте эти три переменные в панели Railway для сервиса
`tutak-api` (Raw Editor), затем напишите — я перезапущу деплой и проверю
журнал старта.

## Статус первого деплоя на момент отчёта

| | |
|---|---|
| Deployment id | `1f504fc3-93b8-47ce-adcf-be18b9a00130` |
| Статус на момент проверки | `BUILDING` |
| Коммит | `df1fd99…` (правильный, ветка `deploy/railway-api-df1fd99`) |

Билд запущен, но ещё не завершён — на момент проверки статус `BUILDING`.
Итог (успех/краш) станет известен позже; без трёх секретов выше ожидаю
краш на старте контейнера, это нормально на этом этапе (соответствует
пункту "Шаг 4 — первый деплой без Viva" из плана).

## 🚨 Важная находка — старый патч НЕ отменён, а вырос

Вы сообщили, что отменили (Discard) старый отложенный патч. Проверка через
API Railway показывает обратное:

- **Тот же `patchId`**: `e0b2070b-341d-4fdb-9590-a3d4e55c78ee`, статус
  по-прежнему `STAGED`.
- **Число изменений выросло с 24 до 37** — то есть он не просто "висит",
  он продолжает накапливать новые изменения, включая только что заданные
  переменные `tutak-api` (все 13 новых переменных сервиса `tutak-api`
  тоже отражены в этом патче с типом `removed`).

Это значит: если этот патч когда-нибудь будет применён (кнопка "Deploy" /
`accept-deploy`), он откатит **и** переменные Postgres/Redis (как раньше),
**и** только что заданные переменные `tutak-api` — то есть сведёт на нет
и текущую настройку API.

Фактическая работа сервиса (сборка, домен, переменные) при этом идёт **не
через этот патч** — `tutak-api` реально собирается (`BUILDING`, настоящий
деплой), и Postgres/Redis не тронуты. То есть прямой угрозы прямо сейчас
нет, но зависший патч — это мина: он существует независимо от моих
действий и продолжает расти. `accept-deploy` я не вызывал и вызывать не
буду без отдельного разрешения.

**Рекомендация**: проверьте в самой панели Railway (не только через API)
раздел staged changes/pending changes для окружения `production` — либо
кнопка Discard не сработала, либо в интерфейсе это выглядит иначе, чем
показывает API. До выяснения — не нажимать ничего похожее на "Deploy
changes"/"Apply"/"Commit" в этом окружении.

## Что дальше

1. Вы добавляете 3 секрета (`JWT_ACCESS_SECRET`, `JWT_REFRESH_SECRET`,
   `SEED_ADMIN_PASSWORD`) в панели Railway для `tutak-api`.
2. Разбираемся со staged-патчом (см. выше) — отдельно от деплоя API.
3. Я проверяю журнал первого деплоя, при необходимости перезапускаю.
4. Дальше — шаг 1 из общего плана (проверка `_prisma_migrations` на чистоту
   базы), если ещё не сделан, и переход к Viva.
