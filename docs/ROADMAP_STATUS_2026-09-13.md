# Статус по дорожной карте от 10.09.2026 — проверено 13.09.2026

Проверено по коду в `main` на `fc1631b7c51e86c07319b9a3bb766422a4973ac3`,
по переменным трёх сервисов Railway и по GitHub API. Где написано «не
сделано» — это результат проверки, а не память.

---

## 1. Что сделано по каждому пункту

### P0 — до первых реальных пользователей

| # | Пункт | Статус | Чем подтверждено |
|---|---|---|---|
| P0-1 | Свести ветки в `main` и защитить его | 🟡 | `main` — боевая ветка, Railway развёрнут с неё (три сервиса на `fc1631b`). Влиты `fix/viva-gateway-path-signing`, `fix/mobile-api-base-url-railway`, `claude/android-keyboard-diagnostics-20260908`, `claude/session-security-fixes-20260907` — проверено `git merge-base --is-ancestor`. **Не влита** `claude/viva-sms-integration-20260908` (4 коммита сверх main). **Ветка по умолчанию всё ещё `claude/tutak-loyalty-mvp-e485jm`**, не `main`. **Защита ветки не настроена** (`/branches/main/protection` → 403). В репозитории 36 веток |
| P0-2 | Обновить `next` ≥ 16.3.3, `sharp` ≥ 0.35.4, `multer` ≥ 2.3.0 | ❌ | В `pnpm-lock.yaml` фактически стоят `next@16.3.0`, `sharp@0.35.3`, `multer@2.2.0`. Диапазоны в `package.json` (`^16.3.0`, `^0.35.3`) допускают новые версии, но lockfile их не поднимал |
| P0-3 | Включить Sentry в проде | ❌ | В `tutak-api` переменной `SENTRY_DSN` нет. В `tutak-admin` есть только `NEXT_PUBLIC_SENTRY_ENVIRONMENT` — без самого DSN он ничего не делает |
| P0-4 | Включить алерты | ❌ | `ALERT_WEBHOOK_URL` в `tutak-api` нет. API сам пишет при старте: «Reconciliation discrepancies, dead-lettered outbox events and failed background jobs will be logged and nothing more — no one will be told» |
| P0-5 | Проверить бэкапы восстановлением | ❌ | Восстановления боевой базы не было. В CI есть шаг «Backup and restore rehearsal» и он проходит — но это репетиция на стенде, не на боевой базе |
| P0-6 | S3 для медиа | ✅ | Заданы все пять `MEDIA_STORAGE_S3_*` плюс `MEDIA_STORAGE_DRIVER` и `MEDIA_PUBLIC_BASE_URL`. `GET /health/ready` отдаёт `{"storage":"ok","storageDriver":"s3"}` |
| P0-7 | Починить CI: таймаут и шардинг | 🟡 | `timeout-minutes: 25` выставлен (`.github/workflows/ci.yml:143`). Шардинг интеграционного набора не сделан — в комментарии рядом помечен как отдельная задача. Отдельно: сегодня CI на `main` красный — из-за флейка в мобильном тесте, исправление в PR #47 |
| P0-8 | Лимиты по IP | ✅ | `CLIENT_IP_STRATEGY` и `CLIENT_IP_TRUSTED_HOPS` заданы в `tutak-api`. В боевом логе старта предупреждения «Request rate limiting is STOOD DOWN» нет — оно осталось только на демо-стенде |
| P0-9 | Гигиена доступов | 🟡 | Переменные `SEED_BASELINE` и `SEED_ADMIN_PASSWORD` в проде присутствуют. Значения мне не видны, поэтому сказать, выключен ли baseline и сменён ли пароль, я не могу — это ваша проверка |
| P0-10 | Политика конфиденциальности и оферта | ❌ | В `docs/` есть только `PARTNER_TERMS.md`. Ни политики конфиденциальности, ни публичной оферты нет |

### P1 — первый месяц

| # | Пункт | Статус | Чем подтверждено |
|---|---|---|---|
| P1-1 | Аналитика продукта | ❌ | Ни PostHog, ни какого-либо другого SDK в коде нет |
| P1-2 | Prometheus + Grafana | 🟡 | `/metrics` реализован, `METRICS_TOKEN` читается в `configuration.ts:517`. В проде переменная не задана, дашборда нет |
| P1-3 | Внешний мониторинг доступности | ❌ | Следов настройки нет |
| P1-4 | Пул соединений и разделение ролей | 🟡 | `connection_limit` поддержан в коде (`database-url.spec.ts`). Разделения нет: боевой веб-инстанс пишет при старте «13 recurring job(s) scheduled», то есть сweep'ы крутятся там же |
| P1-5 | Нагрузочное тестирование | ❌ | Скриптов нет |
| P1-6 | Push включить и проверить | 🟡 | `PUSH_ENABLED` и `PUSH_ENDPOINT` в проде заданы. Сквозной путь до устройства я не проверял |
| P1-7 | Процедура релиза и отката | 🟡 | Есть `docs/RELEASE_2026-09-13_MIGRATION_AND_ROLLBACK.md` — откат конкретных миграций, с замерами. Общей процедуры релиза и отрепетированного отката сервиса нет |
| P1-8 | План реагирования на инцидент и дежурство | ❌ | Нет |
| P1-9 | Ротация секретов, два ключа JWT | ❌ | Поддержки второго ключа в коде нет |
| P1-10 | Индекс документации | 🟡 | `docs/README.md` есть. `docs/archive/` нет, устаревшие аудиты лежат вперемешку — в `docs/` уже больше сотни файлов |
| P1-11 | Починить `pnpm test:unit` без окружения | ❌ | Без `DATABASE_URL` и `JWT_*` набор падает на валидации конфигурации. Сегодня сам обходил это, экспортируя переменные вручную |
| P1-12 | Cookie consent | ❌ | Нет |

### P2 — три месяца

| # | Пункт | Статус |
|---|---|---|
| P2-1 | Боевой эквайринг | ❌ (`CARD_PAYMENTS_ENABLED` в проде выключен — это верно) |
| P2-2 | Повторный пентест | ❌ |
| P2-3 | Выгрузки для бухгалтерии | ❌ |
| P2-4 | Публикация в сторы | 🟡 — профиль `production` (App Bundle, `autoIncrement`) в `eas.json` есть; иконок, скриншотов, текстов и аккаунтов нет |
| P2-5 | Infrastructure as Code | ❌ — `.railway/` нет |
| P2-6 | Кэш «партнёров рядом» | ❌ |
| P2-7 | Feature flags | ❌ |
| P2-8 | Офлайн-кэш в приложении | ❌ |

### P3 — полгода и дальше

P3-1 … P3-7 — ❌, ни один не начат. Это ожидаемо и правильно.

---

## 2. Что сделано с последнего аудита, чего в дорожной карте не было

Дорожная карта была про инфраструктурную зрелость. Сделанное с 10.09 — это
почти целиком продуктовые дыры и настоящие дефекты, найденные по дороге.

**Релиз `122ff52` (PR #45):** заявки партнёров из приложения, очередь заявок
в админ-панели с одобрением и отклонением, имя пользователя при регистрации и
в профиле, показ/скрытие пароля во всех трёх приложениях, ранжирование
партнёров по кешбэку, исправления кошелька, медиа и карты. Отдельно —
**закрыта уязвимость в OTP**: `findFirst` не фильтровал по `attempts`, и
исчерпанный challenge оставался выбираемым, если запись гашения не легла;
вместо 5 попыток он принимал до 15 (`auth-otp.service.ts`,
`otp-consumption-races.int-spec.ts`).

**Релиз `fc1631b` (PR #46), сегодня:** четыре настоящих дефекта ресторанного
сценария, два из них — security:

1. Покупка у партнёра с филиалами могла не принадлежать ни одному филиалу:
   невидима в branch-scoped очередях, но подтверждаема **любым** сотрудником
   партнёра, включая кассира другого филиала.
2. Каталог `GET /partners` отдавал клиентам заявки на рассмотрении и
   отклонённые заведения — по ставке, которую заявитель назначил себе сам.
3. Ресторан было невозможно найти его же фильтром: категория хранилась как
   свободный текст, чтение нормализовало регистр, фильтр — нет.
4. Кабинет партнёра показывал выручку, ничего не зная о возвратах.

Плюс: `PartnerStatus` в общих типах не содержал `SUSPENDED`, хотя API отдаёт
это значение; демо-стенд не мог провести покупку через кассу (не было ни
назначений сотрудников на филиалы, ни QR филиалов); и **две настоящие
взаимные блокировки в тестах** — в `media-and-attempt-races.int-spec.ts` и
`PurchaseIntentStatusScreen.test.tsx`, обе из-за гонки «кто первый дошёл».
Это и был источник «случайных» падений CI.

Написан сквозной ресторанный журней на живом PostgreSQL через контроллеры:
цепочка A→B→C→D, заявка, одобрение, филиал, сотрудник, QR филиала, покупка,
подтверждение кассиром, кешбэк, L1/L2/L3, Referral Challenge, частичный и
полный возврат с clawback, сходимость двойной записи, audit trail.

Подробности: `docs/RESTAURANT_READINESS_2026-09-13.md`,
`docs/RESTAURANT_FLOW_MAP_2026-09-13.md`.

**Из дорожной карты за это время не закрылось ничего нового.** P0-6 и P0-8
были закрыты раньше; P0-7 закрыт наполовину.

---

## 3. Что осталось — по приоритету

| # | Задача | Приоритет | Кто | Часов |
|---|---|---|---|---|
| 1 | Довести CI на `main` до зелёного (PR #47 уже открыт) | P0 | я | 0,5 |
| 2 | Обновить `next` до ≥ 16.3.3, `sharp` ≥ 0.35.4, `multer` ≥ 2.3.0, прогнать всё, передеплоить | P0 | я | 3–5 |
| 3 | Задать `ALERT_WEBHOOK_URL`, проверить искусственным расхождением сверки | P0 | вы (webhook) + я | 3–4 |
| 4 | Включить Sentry: `SENTRY_DSN` в три сервиса, поправить `environment` | P0 | вы (DSN) + я | 3–4 |
| 5 | Проверить бэкапы Railway восстановлением, записать процедуру и время | P0 | вы | 3–4 |
| 6 | Сделать `main` веткой по умолчанию и включить защиту | P0 | вы | 0,5 |
| 7 | Разобрать `claude/viva-sms-integration-20260908` (4 коммита вне main) и убрать мёртвые ветки | P0 | я | 2–3 |
| 8 | Выключить `SEED_BASELINE`, сменить пароль супер-админа, записать реестр секретов | P0 | вы | 1–2 |
| 9 | Политика конфиденциальности и оферта | P0 | вы + юрист | 4–8 |
| 10 | Шардинг интеграционного набора в CI | P0 | я | 2–3 |
| 11 | Разделить сweep'ы и веб-инстансы, задать `connection_limit` | P1 | я | 4–6 |
| 12 | Prometheus + Grafana, `METRICS_TOKEN` | P1 | я | 8–10 |
| 13 | Внешний мониторинг доступности | P1 | вы (аккаунт) + я | 2 |
| 14 | Аналитика продукта | P1 | я | 8–12 |
| 15 | Починить `pnpm test:unit` без окружения | P1 | я | 1 |
| 16 | Сквозная проверка push | P1 | я | 4–6 |
| 17 | Процедура релиза и отката, отрепетировать | P1 | я + вы | 3–4 |
| 18 | Ротация секретов, два ключа JWT | P1 | я | 6–8 |
| 19 | Нагрузочное тестирование | P1 | я | 6–8 |
| 20 | План реагирования на инцидент, дежурство | P1 | вы | 4–6 |
| 21 | Индекс документации, `docs/archive/` | P1 | я | 3–4 |
| 22 | Cookie consent, если ждёте ЕС | P1 | я | 4–6 |
| 23 | Рабочие часы и телефон заведения (нужно ваше решение о формате) | P1 | вы (решение) + я | 8–14 |
| 24 | P2 и P3 целиком | P2/P3 | — | — |

---

## 4. Что блокирует запуск прямо сейчас

**Строгие блокеры — запускать нельзя:**

1. **Нет алертов (P0-4).** Расхождение сверки, мёртвое событие outbox и
   упавшая фоновая задача сегодня попадут только в лог, который никто не
   читает. На платформе, двигающей деньги, это означает, что о проблеме вы
   узнаете от пользователя.
2. **Бэкапы не проверены восстановлением (P0-5).** Бэкап, который ни разу не
   восстанавливали, — это предположение, а не бэкап.
3. **Нет политики конфиденциальности и оферты (P0-10).** Помимо юридического
   риска, без них не пустят ни в App Store, ни в Google Play.
4. **`next@16.3.0` вместо ≥ 16.3.3 (P0-2).** В аудите это помечено как
   critical. Обновление дешёвое, поэтому держать его открытым нечем оправдать.

**Мягкие блокеры — запустить можно, но рискованно:**

5. **Нет Sentry (P0-3).** Об ошибке у пользователя вы не узнаете, пока он сам
   не напишет.
6. **`main` не защищён и не является веткой по умолчанию (P0-1).** Прямой пуш
   в боевую ветку в обход красного CI сегодня технически возможен.
7. **Sweep'ы крутятся на веб-инстансе (P1-4).** Под нагрузкой фоновые задачи
   и запросы пользователей будут драться за один пул соединений.
8. **Нет внешнего мониторинга (P1-3).** Если сервис ляжет целиком, `/health`
   некому опросить.
9. **`SEED_BASELINE` и пароль супер-админа (P0-9).** Не могу проверить сам —
   значения переменных мне не видны.

---

## 5. Что требует вас

| Что | Что конкретно нужно | Куда вставить |
|---|---|---|
| **Webhook для алертов** | URL входящего вебхука Telegram-бота или Slack | Railway → `tutak-api` → Variables → `ALERT_WEBHOOK_URL` |
| **Sentry DSN** | Завести проект на sentry.io, взять DSN для бэкенда и для мобильного/веб | Railway → `tutak-api` → `SENTRY_DSN`; `tutak-admin` и `tutak-partner` → `NEXT_PUBLIC_SENTRY_DSN` |
| **Бэкапы Railway** | Убедиться, что включены; восстановить в отдельную базу; записать, сколько заняло | Панель Railway → Postgres → Backups |
| **Ветка по умолчанию** | Переключить на `main` | GitHub → Settings → General → Default branch |
| **Защита ветки** | Запрет прямого пуша в `main`, обязательный зелёный CI | GitHub → Settings → Branches → Add rule |
| **Гигиена доступов** | Выключить `SEED_BASELINE`, сменить `SEED_ADMIN_PASSWORD`, записать реестр секретов | Railway → `tutak-api` → Variables |
| **Юридические документы** | Политика конфиденциальности и публичная оферта у юриста | Опубликовать и дать ссылки для приложения и панелей |
| **Аккаунт мониторинга** | Better Uptime или UptimeRobot | Дадите доступ — настрою |
| **Старый ключ MapTiler** | Отозвать | https://cloud.maptiler.com/account/keys/ |
| **Решение: формат рабочих часов** | Пара «открытие/закрытие» на день; или список интервалов (перерывы, ночные смены); или интервалы плюс исключения на праздники | Влияет на миграцию — поэтому не угадываю |
| **Решение: нужен ли ЕС** | От этого зависит, делать ли cookie consent и насколько строго GDPR | — |

---

## 6. Сколько осталось до полной готовности P0

Считаю только незакрытые P0, по пунктам 1–10 таблицы раздела 3.

| | Моих часов | Ваших часов | Календарно |
|---|---|---|---|
| **Минимум** (всё идёт гладко, вы отвечаете сразу) | 11 | 9 | 2–3 дня |
| **Реалистично** (обновление зависимостей ломает тесты, вебхук и Sentry настраиваются со второго раза, юрист отвечает не в тот же день) | 18 | 14 | 5–8 дней |

Важно: **мои 18 часов и ваши 14 не складываются в 32 календарных.** Пункты
3, 4, 5, 6, 8 упираются в вас и блокируют мои — пока нет DSN и вебхука, я
могу подготовить код, но не могу проверить, что оповещение дошло. Юрист
(пункт 9) идёт параллельно и ни от чего не зависит — его стоит запустить
первым же действием, потому что у него самый длинный срок ответа.

P1 целиком — ещё 50–70 моих часов и 6–10 ваших. P2 — от 130 часов плюс
договор с банком.

---

## 7. Мой следующий шаг

Довести CI на `main` до зелёного: PR #47 уже открыт и ждёт прогона. Пока он
красный, любое следующее изменение сливается вслепую, а защиту ветки
(обязательный зелёный CI) включать бессмысленно.

Сразу после него, не спрашивая: **P0-2 — обновить `next` до ≥ 16.3.3,
`sharp` до ≥ 0.35.4 и `multer` до ≥ 2.3.0**, прогнать весь набор, передеплоить
и пересобрать APK. Это единственный строгий блокер, который целиком в моих
руках: ключей не требует, решений не требует, а помечен как critical.

А вам стоит первым делом отдать текст юристу — это самый долгий срок из всего
списка, и он ни от чего не зависит.
