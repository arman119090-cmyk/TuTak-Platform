{
  "name": "@tutak/observability",
  "version": "0.1.0",
  "private": true,
  "description": "Shared Sentry event/breadcrumb sanitization policy for the mobile app and both web dashboards. apps/api keeps an equivalent copy under its own rootDir constraint — see apps/api/src/common/observability/sentry-sanitize.ts — and is tested there instead.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  }
}
