# Asset notes

- `assets/source_screenshots` contains all 19 untouched screenshots supplied by the owner.
- `assets/products/jpg` and `assets/products/webp` contain crops with Instagram chrome removed as far as the available screenshot allows.
- The Sea Turtle source is only partially visible in the supplied screenshot; do not invent/crop-expand missing parts with generative fill.
- Logo files are reference crops from the supplied social profile screenshot. They are not a vector master and should be replaceable later.
- If higher-resolution originals become available, replace the product files while keeping the same slugs/paths.
